# R159 — 2026-06-28 02:10 UTC

## Summary
P0 multi-source data drift + HR#40 zombie. Standard parallel-agent collision repair round.

## Findings
1. **P0 HR#13/15/31**: 5-source drift — GAMES=468 but HTML data-count=467, JSON-LD numberOfItems=467, manifest=466 (2 behind). Sling Smash (#468) added to GAMES array by pipeline but NOT synced to HTML/JSON-LD/manifest/about.
2. **P0 JSON-LD partial-sync**: Block0 + `js/itemlist-schema.js` both missing Sling Smash entry (467 items, need 468). Pre-commit hook caught itemlist-schema.js drift (inline Block0 was fixed first, hook flagged the schema file).
3. **P0 Puzzle cat-count micro-drift**: GAMES Puzzle=382 but HTML cat-count=381. Sling Smash is cat:"puzzle" — pipeline didn't update per-category count. Targeted `<span class="cat-count">381</span>`→382 (protected position:381 in JSON-LD).
4. **P1 HR#40 60th**: sling-smash 1ktower zombie (partial-violation — trio intact, only 1ktower active).
5. **R155 standing instruction 3rd catch**: 3 asymmetric `467%2B` share URLs (Twitter×2 + WhatsApp). Phase 1.5 HR#15 reported "0 stale" but missed %2B-encoded URLs.

## Fixes Applied (commit `638c3b39ffd`)
- index.html: JSON-LD Block0 add Sling Smash pos#468 + numberOfItems→468 (compact serialize)
- index.html: global 467→468 (35 stale text sites, `"position":467` protected with digit-free placeholder)
- index.html: Puzzle cat-count 381→382 (targeted span replace)
- js/itemlist-schema.js: add Sling Smash pos#468 + numberOfItems→468 (compact serialize)
- manifest.json: 466→468 (name + description, 2 sites)
- about.html: 466→468 (7 sites)
- sling-smash/index.html: 1ktower zombie removed (HR#40 60th)
- Cache-bust: 4 stale ?v= values unified to `?v=20260628R159`
- Absorbed: tunnel-rotation URL updates (400+ game dirs) + pipeline sling-smash/nail-art

## Verification
- Phase 1.5: ALL 10/10 PASS ✅
- Full-site HR#40: 468/468 OK ✅
- R155 %2B scan: 0 stale ✅
- Pre-commit hook: ✅ all checks passed
- Production curl: numberOfItems=468, 3× 468%2B share URLs, H1 "468 games" ✅

## New Micro-Pattern
**Per-category cat-count drift** (distinct from total-count drift): pipeline adds a game to GAMES array with cat:"puzzle" but doesn't update the per-category `<span class="cat-count">N</span>` in HTML. Total count (468) was correct but Puzzle category was 381 vs 382. The `sync-game-counts.sh` script's per-category check caught this. Fix: targeted replace on the specific cat-count span (NOT global bytes.replace — would corrupt position:381 in JSON-LD).
