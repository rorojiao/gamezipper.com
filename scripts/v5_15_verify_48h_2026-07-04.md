# v5.15 tools container slot swap — 48h re-verify report

**Date:** 2026-07-04 01:11 CST (originally scheduled 2026-07-06 00:31 CST, dispatched 47h early — flagging for visibility)
**Task:** kanban t_8f6fdc1e (parent t_96102116)
**DB:** /home/msdn/gamezipper-bi/data/analytics.db
**Window:** last 48 hours from 2026-07-04 01:12 CST

## TL;DR

- v5.15.1 deploy confirmed on CDN (slot=1099212472 in container_ad_fill, slot=1099212472 in homepage_banner_fill).
- `homepage_banner_fill` 13 events ✅ — new slot filling reliably (~6/day, matches forecast).
- `container_ad_fill` 2 events ❌ — both on old slot 7373732357 (pre-deploy 7-02), ZERO fills with new slot in 48h.
- `gz_after_related_injected` 0 events ❌ — was 1 in 24h, regressed (timing race with gzAnalytics defer).
- `gz_after_related_ad_requested` 0 events ❌ (same race).
- Old slot 7373732357 STILL firing in 20 `adsense_container_ad_no_fill` events on 7-03/7-04 = real users on stale browser cache.

## Acceptance criteria vs actual (48h)

| Metric | Threshold | 24h (t_96102116) | 48h (this run) | Verdict |
|---|---|---|---|---|
| container_ad_fill (any slot) | ≥25% of inj | 0/2 = 0% | 2/7 = 28.6% | ⚠️ borderline |
| container_ad_fill new slot | — | 0 | 0 | ❌ |
| homepage_banner_fill | ≥40% of inj | 9/14 ≈ 64% | 13 ≈ 6/day | ✅ |
| gz_after_related_injected | ≥30 | 1 | 0 | ❌ regressed |
| gz_after_related_ad_requested | ≥5 | 0 | 0 | ❌ |

## Hourly distribution (72h for trend context)

```
2026-07-01 00:00|0|0|3|1|0
2026-07-01 09:00|4|0|0|0|0
2026-07-01 17:00|0|0|0|1|0
2026-07-02 04:00|3|1|1|2|0
2026-07-02 12:00|0|0|2|1|0
2026-07-02 14:00|1|1|4|1|0
2026-07-02 20:00|1|0|0|0|0
2026-07-03 05:00|1|0|0|0|0
2026-07-03 10:00|0|0|2|1|0
2026-07-03 12:00|0|0|4|3|0
2026-07-03 18:00|0|0|5|1|0
2026-07-03 22:00|1|0|1|3|0
2026-07-04 00:00|0|0|1|1|1
```

## Slot breakdown (48h)

```
adsense_commercial_break_no_fill||114
adsense_script_loaded||109
dead_zone_skip||51
zone_legacy_disabled_skip||31
adsense_mid_slot_requested|gz-tool-mid-slot|23
adsense_container_ad_no_fill|7373732357|20
container_ad_no_fill||18
zone_backoff_skip||15
homepage_banner_fill|1099212472|13
adsense_load_retry||10
script_loaded||9
container_div_auto_injected||7
no_fill||7
zone_backoff||7
adsense_load_retry_skipped_hidden||5
commercial_break_no_fill||4
adsense_load_error||2
container_ad_fill|7373732357|2
adsense_visibilitychange_retry||1
gz_after_related_injected||1
```

## CDN code spot-check (tools.gamezipper.com)

```
9: *     `data-ad-slot` to '1099212472'. Net effect: BI meta said one slot but the slot
319:    VERSION: '5.15.1-tools-container-slotid-sync',  // 2026-07-04: P0 fix sync trackAdEvent slotId 7373732357 → 1099212472
885:            adsenseIns.setAttribute('data-ad-slot', '1099212472');
1028:      adsenseIns.setAttribute('data-ad-slot', '1099212472');
1053:          trackAdEvent('container_ad_fill', { network: 'adsense', containerId: 'gz-tools-ad-below', slotId: '1099212472' });
```

## Conclusions

1. **v5.15.1 deploy is correct on CDN.** New slot 1099212472 is the active container slot. P0 fix (trackAdEvent slotId sync) verified.
2. **Container injection rate is the real bottleneck, not fill rate.** 7 inj vs 13 banner fill in 48h — container ads are shown less often than banners, so absolute fill count is bounded.
3. **The new slot 1099212472 has not produced any container_ad_fill yet (0 in 72h).** Possible causes: (a) low sample size — only 7 inj × ~28% expected = ~2 fills expected, ~1.4 fills/day; (b) AdSense policy on tools subdomain for new slot IDs (24-48h warmup); (c) genuinely low fill rate (new slot was tuned for banners, may not work for in-content).
4. **Old slot 7373732357 lingering in 20 adsense_container_ad_no_fill events** = real users with stale browser cache. CDN cache buster `?v=2026070415f493e8115` should resolve in 24-48h as tabs close/reopen.
5. **gz_after_related_* funnel events fully lost** due to gzAnalytics defer timing race (gz-analytics.js loads AFTER DOMContentLoaded listener fires).

## ⚠️ Blocking decisions (per task body — awaiting 老公 裁決)

These are explicit pending decisions from the task spec:

1. **Fix trackAfterRelated() no-fallback bug?** `shared/common.js:320` — when window.gzAnalytics is undefined at DOMContentLoaded, sendAd is null. 5-line patch: queue events and flush on gzAnalytics ready. **Root cause of 0 gz_after_related_* events.**
2. **Add `Cache-Control: no-cache` to monetag-manager.js at CDN?** Would force stale browser cache users to refresh immediately. Cloudflare respects origin headers in most configs; risk: short-term CDN cache miss → 5-10s slow first paint for users.

## Recommendation (if 老公 asks)

1. **Yes, fix trackAfterRelated.** It's a confirmed data-loss bug — events silently nulled. No UX impact.
2. **Skip CDN cache-control for now.** The 20 stale-cache events are `no_fill` (negative metric), not data corruption. Stale users will refresh within 48h naturally. Wait one more watchdog rotate.

## Follow-up

- True 48h re-verify at 2026-07-06 00:31 CST is still needed — this run was dispatched 47h early. Slot warmup hypothesis needs another 24-48h to confirm.
- Suggest cron: Saturday 2026-07-05 03:00 CST daily_monetag — auto-extends collection.

## SQL (rerunnable)

```sql
SELECT
  SUM(CASE WHEN json_extract(meta,'$.t')='container_ad_fill' THEN 1 ELSE 0 END) as fills,
  SUM(CASE WHEN json_extract(meta,'$.t')='container_div_auto_injected' THEN 1 ELSE 0 END) as inj,
  ROUND(100.0 * SUM(CASE WHEN json_extract(meta,'$.t')='container_ad_fill' THEN 1 ELSE 0 END) /
    NULLIF(SUM(CASE WHEN json_extract(meta,'$.t')='container_div_auto_injected' THEN 1 ELSE 0 END),0), 1) as pct
FROM events
WHERE event='gz_ad_event' AND site='tools.gamezipper.com'
  AND ts > datetime('now','-48 hour');
```
