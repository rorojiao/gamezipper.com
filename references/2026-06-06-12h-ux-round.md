# 2026-06-06 12h UX Round

## Status: PURE P0 REPAIR (Hard Rule #7 short-circuit triggered)

**Phase 1 web_search was SKIPPED** — Phase 1.5 regression check caught 5-source data drift + 14 user-visible stale text sites. The entire 40-min budget went to repair. 0 features were lost because the round's job was to keep the site alive + crawler-consistent, not improve it.

## Drift Caught (cumulative count: 9 in 8 days)

**5-source data drift (Hard Rule #13)**:
- GAMES array: **255** (truth)
- JSON-LD numberOfItems: 253 → fixed to 255
- JSON-LD itemListElement.length: 253 (after split('},{')) → 254 actual items → 255 after Growmi added
- HTML All cat-count: 253 → 255
- HTML Puzzle cat-count: 178 → 180 (Grow Worm +1, Growmi +1)
- Header data-count: 253 → 255
- Footer data-count: 253 → 255
- HTML missing **Action** category button entirely (Glass Rush 3D's cat:action)

**14 user-visible stale text sites (Hard Rule #15)**:
1. H1 "Free Online Games — 253 Games" → 255
2. og:image:alt "GameZipper - 253 Free Online Browser Games" (2x — og + twitter)
3. twitter:title "Free Online Games 253 — Play 253..." (2x in one line)
4. twitter:description
5. VideoGame JSON-LD name
6. WebPage JSON-LD name
7. itemlist-schema cache-bust `?v=10h-fix-253` → `?v=12h-fix-255`
8. noscript H2 "253 Free Online Games"
9. Hero subtitle `<p>253 free unblocked...</p>`
10. Search placeholder × 2 (HTML attr + JS focus listener)
11. no-games fallback "253 free games ready to play"
12. load-status aria-live span "Loading 253..."
13. Trust bar "253 Games — All free"
14. Blog CTA "🎮 253 free games — No Signup, No Download"
15. H2 "Why Play 253 Free Online Games on GameZipper?"
16. navigator.share title
17. Twitter share URL `GameZipper%20-%20253%2B`

## Root Cause

Two new games added since the 10h round:
1. **Glass Rush 3D** (`fc6b3b35`) — uses NEW category `action` not present in HTML
2. **Grow Worm** (`f97054d7`) — added to GAMES but never propagated to JSON-LD or HTML cat-counts

The 5th place in the drift — Growmi — was already in GAMES (cat:puzzle) from a prior round but never added to JSON-LD schema. Schema had 254 items, should be 255.

## Fixes Applied (commit `754e2214`)

1. **index.html** (+173 bytes):
   - Added Action button: `<button class="cat-btn" onclick="filter('action',this)">💥 Action <span class="cat-count">1</span></button>`
   - Puzzle 178 → 180
   - All 253 → 255
   - H1 253 → 255
   - Header data-count 253 → 255
   - Footer data-count 253 → 255
   - 14 user-visible text sites 253 → 255 (with new ?v=12h-fix-255 cache-bust)

2. **js/itemlist-schema.js**:
   - Added Grow Worm at position 254
   - Added Growmi at position 255
   - numberOfItems 253 → 255

3. **scripts/sync-game-counts.sh** (bug fix):
   - `split('},{')` on compressed JSON-LD gave wrong count (253 instead of 255)
   - Switched to counting `"@type":"ListItem"` occurrences — always correct

4. **scripts/sync-user-visible-text.sh** (Hard Rule #15) — CREATED
   - Validates 6 high-traffic user-visible text sites (H1, META desc, search placeholder, footer label, wide-net stale scan)
   - Exits 1 on drift
   - Filters out CSS noise (rgba, @keyframes, font-size) + JS comments (//)

5. **scripts/install-pre-commit-hook.sh** (Hard Rule #17) — CREATED
   - Installs `.git/hooks/pre-commit` that runs:
     1. `node -c js/games-data.js` (catches all 6 orphan variants)
     2. `bash scripts/sync-game-counts.sh` (catches 3-place data drift)
     3. `bash scripts/sync-user-visible-text.sh` (catches user-visible text drift)
   - Backs up existing non-gamezipper hooks to `pre-commit.bak.<timestamp>`
   - Verified passing with `git commit --allow-empty -m test`
   - Would have caught ALL 7 prior drift incidents at write-time

## Pre-commit Hook Test

```
$ git commit --allow-empty -m "test: verify pre-commit hook"
[gz-pre-commit] Running Hard Rule checks...
[gz-pre-commit] ✅ All checks passed.
[main 9e4a42cb] test: verify pre-commit hook
```

Hook works. (Test commit soft-reset before final commit.)

## Live Verification (Kachilu DOM, after Cloudflare 90s wait)

```
h1: "Free Online Games — 255 Games, No Download | ⚡ GameZipper"  ✅
all_count: "255"  ✅
card_count: 24  ✅
data_count_header: "255"  ✅
data_count_footer: "255"  ✅
puzzle_cat: "180"  ✅
```

Cloudflare `last-modified: 2026-06-06 09:02:31` (after my commit). 5 min refresh delay expected (Hard Rule #8).

## Hard Rule #18 (proposed, next round)

**3-Place Data Drift Phase 1.5 must also re-run after every game-add commit, not just every 2h cron.** The 2h cadence misses the window between commit and next cron — a user could see 253 in search snippets for up to 2h after a new game is added. Pre-commit hook catches this at write-time (now installed), but the next-round `gen-sitemap` and `seo_daily` automation should ALSO call `bash scripts/sync-game-counts.sh` and `bash scripts/sync-user-visible-text.sh` after every game-add commit, and fail the CI if they detect drift.

## Cumulative drift count: 9 in 8 days

- 2026-05-31: 4 simultaneous stale game counts
- 2026-06-05 12h: orphan object literal (P0)
- 2026-06-05 22h: orphan Variant D
- 2026-06-06 00h: 4-source data drift
- 2026-06-06 02h: puzzle HTML 174 vs GAMES 175
- 2026-06-06 06h: 11 user-visible stale text sites
- 2026-06-06 08h: orphan Variant E + 3 stale descriptions
- 2026-06-06 10h: orphan Variant F + 13 user-visible text
- **2026-06-06 12h: 5-source data drift + 17 user-visible text sites** ← this round

## Action Item Completed

✅ Pre-commit hook installed — would catch all future orphan + drift incidents at write-time.
✅ sync-user-visible-text.sh script created — runs in <2s, validates 6 high-traffic sites + wide-net scan.
✅ sync-game-counts.sh script bug fixed — split('},{') replaced with ListItem count regex.
