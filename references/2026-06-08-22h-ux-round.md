# 2026-06-08 22h UX Round

## P0 Fixes

### 1. JSON-LD ItemList Block 0 Structural Corruption
**Severity**: P0 (SEO — Google Search Console would flag invalid structured data)
**Root cause**: Parallel agent commit `b7e0044c` (feat: add masyu) appended the masyu ListItem entry OUTSIDE the `itemListElement` array — after the closing `]` and `numberOfItems`/`description` fields. The resulting JSON was:
```
...itemListElement:[...52 items...],"numberOfItems":54,"description":"54 free...",{"@type":"ListItem","position":53,"url":"...masyu/","name":"Masyu"}}
```
This is a new variant of JSON-LD corruption — not a parse error per HR#22, not a semantic drift per HR#36, but a **structural corruption** where the new entry was placed at the wrong nesting level.

**Fix**: Python script moved masyu entry back into the array (before `]`), fixed `numberOfItems` 54→53, fixed description "54"→"53".
**Detection**: `json.loads()` on Block 0 threw `Expecting property name enclosed in double quotes` — the dangling object after description field created invalid JSON.

### 2. HR#39 Violation (8th occurrence) — Masyu Missing from All 3 Prefetch Lists
**Severity**: P0 (perf — S-grade game not prefetched)
**Root cause**: Same pattern as 00h/02h/04h/06h/08h/10h/12h/14h/16h/18h/20h rounds. Parallel agent commit `b7e0044c` added masyu game but did not update:
- Static `<link rel="prefetch">` list (was 20 games)
- JS `topGames` array (was 20 games)
- SW `precacheURLs` array (was 33 URLs)

**Fix**: Added `/masyu/` to all 3 lists. Static 20→21, JS 20→21, SW 33→34.
**SW version bump**: gz-v22→gz-v23.
**Cache-bust**: 2026-06-08-20h → 2026-06-08-22h (4 params).

## Phase 1 Research Results
3 keywords searched, 8 new techniques found. ALL already implemented or infrastructure-only:
- 103 Early Hints → infrastructure (Cloudflare config, not code)
- Font Metric Override Descriptors → site uses system fonts, no custom web fonts
- LoAF API → already have `requestIdleCallback` LoAF monitoring (21 instances)
- AVIF → infrastructure (needs build pipeline, not a code fix)
- CSS `contain: layout` → already have `content-visibility:auto` (32 instances) which subsumes this
- Facade Pattern → no heavy 3rd-party scripts to facade (analytics is lightweight)
- Mobile srcset → game thumbnails are CSS background-images, not `<img>` tags
- Critical CSS Inline → already inlined (all CSS is inline in `<style>` tag)

**Site at near-optimal 2026 state (7th consecutive round).** Remaining wins are infrastructure-level only (CF Early Hints, AVIF build pipeline, Brotli-11).

## Verification
- `sync-game-counts.sh`: ALL IN SYNC (276) ✅
- `sync-user-visible-text.sh`: ALL IN SYNC (276) ✅
- JSON-LD all 4 blocks: valid, numberOfItems matches ✅
- Static prefetch: 21 games ✅
- JS topGames: 21 games ✅
- SW precache: 34 URLs ✅
- Masyu game-page integration (HR#40): game-footer.js + monetag-manager.js + gz-ad-below-game all present ✅
- Production curl verification: all fixes deployed, last-modified matches commit time ✅
- Pre-commit hook: all 6 checks passed ✅

## Commit
- `c2c37ff7` — fix: JSON-LD ItemList structural corruption + HR#39 prefetch fix

## Cumulative Status
- Cumulative drift count: 13 in 9 days
- HR#39 violations: 8 total (one per game-add, every 2h)
- Site optimization: near-optimal for 2026 (7th consecutive round of research confirming all techniques implemented)
