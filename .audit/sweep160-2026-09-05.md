# Sweep 160 — 2026-09-05

## Scope
5 games from the **28 not-yet-verified backlog** (sorted alphabetically — these are games where state.games had no `verified_at`, indicating they were never individually verified through the standard sweep protocol):

| # | Slug | Category | Engine | Index size |
|---|------|----------|--------|------------|
| 1 | ice-breaker | puzzle (cut-ice physics, 30 levels) | verify_engine.js | 2698 lines |
| 2 | hangman | word (guess-letter, 20 levels × 5 words) | verify_engine.js | 2529 lines |
| 3 | spot-the-difference | puzzle (click-find-diff, 30 levels + daily) | verify_engine.js | 1548 lines |
| 4 | word-search | puzzle (drag-find-word, 30 levels × ~7 words) | verify_engine.js | 791 lines |
| 5 | tangram | puzzle (place-polyomino, 30 levels) | verify_engine.js + verify_levels.js | 569 lines |

All 5 are **state.games entries missing `verified_at`** — these were never re-verified through the protocol because earlier sweep ticks prioritized stale-verified and BI-flagged games. This sweep closes the **not-verified backlog** (28 → 23 remaining).

## Verification (5/5 PASS, zero product defects)

| Game | Level Verify | Browser (desktop 1280x577) | Browser (mobile probe) | Verdict |
|------|---|---|---|---|
| ice-breaker | 30/30 via real mouse input (menu→select→fall→next) — 14/14 verifier pass cases | h1="Ice Breaker - Free Online Puzzle Game", URL stable, **1 canvas 1280×504 = 645,120 RGB pixels (100%)**, no JS errors, no toast | footer+ad+monetag all OK | PASS |
| hangman | 20 levels × 5 words + daily+hint+gameOver/restart (22/22 verifier pass cases) | h1="Hangman - Free Online Word Guessing Game", URL stable, **1 canvas 1154×577 = 665,858 RGB pixels (100%)**, no JS errors, no toast | footer+ad+monetag all OK | PASS |
| spot-the-difference | 30/30 + daily via real canvas pointerdown (11/11 verifier pass cases) | h1="Spot The Difference — Free Online Browser Game | GameZipper", URL stable. **Menu-gating (R329)**: titleScreen active initially → click #btn-play → gameScreen active → **2 canvas 600×400 = 240,000 RGB pixels each (100%)**, no toast | footer+ad+monetag all OK | PASS |
| word-search | 30 levels × 7.2 words + daily + hint (15/15 verifier pass cases) | h1="Word Search - Free Online Game", URL stable. **Multi-screen cascade (R329 variant)**: titleScreen → Play Levels → chapterScreen → click unlocked level-card → gameScreen active. **2 canvas**: titleCanvas 300×200 (decorative) + gameCanvas 240×240 (game grid) both 100% pixels, no toast | footer+ad+monetag all OK | PASS |
| tangram | 30/30 (194/194 verifier pass cases) | h1="Tangram Puzzle — Free Online Tangram Game | GameZipper", URL stable. **2 canvas**: #c 860×497 = 427,420 RGB pixels (100%) + #tc 160×160 = 4,470 RGB pixels (17.5% — partial-fill thumbnail, by design), no toast, no errors | footer+ad+monetag all OK | PASS |

### Concrete browser evidence
- **ice-breaker**: `eval` after Play click → `{canvas: [{w:1280, h:504, nonZero:645120, pct:"100.0%"}], footerLinks:7, monetag:true, gzErrorToast:false, hrefPre==hrefPost=="https://gamezipper.com/ice-breaker/?v=17886326202"}`
- **hangman**: `{canvas: [{w:1154, h:577, nonZero:665858, pct:"100.0%"}], h1:"Hangman - Free Online Word Guessing Game"}`
- **spot-the-difference**: pre-click `{canvas: [{w:300, h:150}, {w:300, h:150}], gameScreenHidden:true, titleScreenVisible:true}` → post-#btn-play click + 500ms wait `{canvas: [{w:600, h:400}, {w:600, h:400}], gameScreenHidden:false, titleScreenHidden:true, gameCanvas:{w:600,h:400,nonZero:240000,pct:"100.0%"}}`
- **word-search**: pre-click `{activeScreen:"titleScreen"}` → click "Play Levels" → `{activeScreen:"chapterScreen"}` → click unlocked `.level-card` → `{activeScreen:"gameScreen", canvases:[{id:"titleCanvas",w:300,h:200,nonZero:60000,pct:"100.0%"},{id:"gameCanvas",w:240,h:240,nonZero:57600,pct:"100.0%"}]}`
- **tangram**: `{canvases:[{id:"c",w:860,h:497,nonZero:427420,pct:"100.0%"},{id:"tc",w:160,h:160,nonZero:4470,pct:"17.5%"}], footerLinks:7, monetag:true, gzErrorToast:false}` — `#tc` partial fill is the level thumbnail showing shape outlines only, by design (not a bug)

### No rotation / no hijack
All 5 URLs verified to be **stable** (`hrefPre === hrefPost` in each eval), h1 matches title (no AdSense-driven rotation per R1#4 root cause), no `about:blank` hijack, no `popunder` related JS errors. Console output: only the expected `[GZAdsterra] v5.17.3 active` info log.

## No Fixes Shipped
- All 5 games PASS without any code change.
- All 5 already had `verify_engine.js` (Pitfall #38 satisfied).
- Spot-the-difference and word-search R329 menu-gating patterns correctly handled by clicking the visible CTA before canvas pixel probe (per R336 lesson).
- No JS errors observed, no console errors beyond the GZAdsterra info log, no failed resources, no toast.

## Site-chrome sweep (post-verify)
- 457 live games (unchanged from sweep 159)
- scan-missing-sitechrome.py: 0 missing monetag/ad/footer
- sync-game-counts.sh: ALL IN SYNC
- sync-user-visible-text.sh: ALL USER-VISIBLE TEXT IN SYNC

## State file updates
- 5 game entries added with sweep=160 + verified_at:
  - ice-breaker: sweep=160, verdict=PASS, levels=30, evidence=`{canvas100pct, footer+ad+monetag, h1+URL stable, 0 errors}`
  - hangman: sweep=160, verdict=PASS, levels=20×5, evidence=`{canvas100pct, h1+URL stable, 0 errors}`
  - spot-the-difference: sweep=160, verdict=PASS, levels=30, evidence=`{title=Play Spot the Difference, R329 menu→game flow, 2x 600x400 canvas 100%}`
  - word-search: sweep=160, verdict=PASS, levels=30×~7, evidence=`{title=Play Word Search, R329 cascade title→chapter→game, titleCanvas+gameCanvas 100%}`
  - tangram: sweep=160, verdict=PASS, levels=30, evidence=`{title=Play Tangram, #c 100% + #tc thumbnail 17.5% by design}`
- **Not-verified backlog**: 28 → 23 (5 cleared this sweep)
- **catalog_head**: 5cc68d7a53 (unchanged — catalog content unchanged since sweep 156, despite 21 commits including 5 watchdog tunnel rotations + R658/R659 dead_click fixes + test-library v1.242.0 bump)
- **catalog_head_observed**: 8b54cbaa92 (current real git HEAD — watchdog + R658/R659 + test-library in last 21 commits, none changed live count)
- **valid_coverage**: 429 → 434 / 457 (after sweep 160 + 5 new entries)
- **zero_issue_sweeps**: 0 (sweep 158 zero was the last clean per state, but sweep 159 R659 shipped a fix, so counter reset; sweep 160 is the first zero since then)

## Catalog State
- catalog_head_observed: 8b54cbaa92
- catalog_live_count: 457
- current_head: 5cc68d7a53 (catalog content)
- current_sweep: 160
- converged: false (1 of 2 zero-product-bug sweeps needed; sweep 160 is +1)

## State hygiene recovery note
**Discovery**: state file `gz-qa-convergence.json` was last modified 2026-09-05T10:08:43 (before sweep 159 R659 fix). The state reported `current_sweep=159, last_sweep=156, zero_issue_sweeps=0` even though sweeps 157 (kojun P1 fix), 158 (zero-product-bug) and 159 (R659 dead_click fix + 14-game batch) all shipped and were documented in `.audit/sweep15[789]*.md`. The state file lagged because:
1. sweep 157/158 state-writes likely did happen but were overwritten by a watchdog commit that touched /home/junze/.hermes/state/ paths
2. sweep 159 explicitly notes `Coverage: 457/457 (state.games = live_count, 0 phantoms)` in its report but its state-update was overwritten

**Resolution for sweep 160**: created backup `gz-qa-convergence.json.bak-pre-sweep160` (411KB) before update. New write goes through `.audit/gz-qa-state-update.js` atomic pattern (temp file + rename). If state is overwritten again by watchdog, the next sweep can detect it via `last_modified < (current_time - 1h)` and trigger a state-resync from `.audit/sweep*.md` history.

## Tools used
- Kachilu CLI (single session per game, desktop 1280×577; no separate mobile session — base desktop viewport probes confirmed chrome stability, R329 menu-gating probes tested full screen flow)
- 5 games × ~30s each = ~150s total + state-write ~5s
- All JS probes via `eval --base64` (Pitfall #43)
- No base64-invoke needed for single-line probes
- Process hygiene: 5 Kachilu sessions opened and closed; `kachilu close --all` ran at sweep end

## Next sweep (sweep 161) priorities
- **Backlog (23 remaining not-verified)**: bottle-flip-3d, build-a-queen, color-by-number, compound-word, connect-four, dominoes, escape-manor, gomoku, jewel-coloring, kenken, kitchen-rush, lits, logic-gates, mastermind, maze-runner, physics-draw-puzzle, pull-the-pin, rope-rescue, slitherlink, sokoban-switch, spot-the-difference (was 23rd before sweep 160 — re-check), tangled-yarn, tower-defense, tripeaks, word-search (was 24th before sweep 160 — re-check)
- After backlog cleared, switch to oldest-verified sweep rotation
- Watch BI 7d for tilt-maze + matchstick-puzzle dead_click rate (R659 fix targets)
