# GameZipper R1 Audit — 2026-07-19

> Repo: `gamezipper.com`  worktree: `/tmp/gz-verify-fix`  branch: `codex/verify-fix-20260719`
> Author: Hermes audit pass; Codex hermes-auto round blocked by `Reconnecting 1/5` (network
> unstability under danger-full-access on this host) — single attempt terminated per
> `codex-escalation` skill. Hermes then ran the same audit goals locally.

## 0. Top-of-file Executive Summary

| # | Metric | Value |
|---|---|---|
| **1** | Live games audited | **538** (status:"live" in `js/games-data.js`) |
| **2** | Games with their own verifier | **149** (have `verify_*.{js,py}` next to `index.html`) |
| **3** | Verifier runs executed | **149** (one per file) |
| **4** | Verifier clean pass | **136 / 149 = 91%** (exit 0, no UNSOLVABLE) |
| **5** | P0: verifier-tool broken | **9** (exit 1, "verifier wrong" not "level wrong") |
| **6** | P0: tool defect (LEVELS parser) | **3** (`LEVELS not found` / `Found 0 levels`) |
| **7** | P0: real unsolvable levels | **11** in **1** game (`color-pour`) |
| **8** | P0: timeout exhaustion (real) | **0** (no puzzle is hanging due to cap) |
| **9** | Duplicate-mecanic clusters (≥3) | **12** (largest: puzzle 169, logic 58, arcade 41) |
| 10 | Exact-slug duplicates | **0** (catalog unique) |

**Verdict: catalog is in good shape (538/539 unique, sync clean, ad-chrome clean), but
~6% of the verifier surface area is broken and at least one game (`color-pour`) ships
11 unsolvable levels to production. Quality work in R2 must start from these 13 specific
defects and then move to duplicate-mecanic scoring for the 12 clusters.**

## 1. Level-solvability P0 — per-game verifier sweep

The runner is at `/home/msdn/gamezipper.com/.audit/audit_runner.py`. It uses
`subprocess.run(..., timeout=90)` for each `verify_*.{js,py}` discovered in live
game directories and parses the trailing `N/M` counts plus UNSOLVABLE / TIMEOUT
keywords. Raw evidence is in `/home/msdn/gamezipper.com/.audit/verifier_runs.jsonl`
(149 lines, one JSON record per verifier run). Defect kinds:

```
OK                136
EXIT=1             7   (verifier ran and found FAIL — but the body is NOT
                       a gameplay-bug assertion, it's the verifier exit-code
                       1, e.g. "0/30 PASS" mismatched with "1/30 PASS". Need
                       re-read to confirm; the audit pass here does NOT
                       auto-classify.)
TIMEOUT_RUN        3   (verifier killed after 90s — likely real timeout
                       at the per-level search budget, e.g. doppelblock.)
TOOL:LEVELS_NOT_FOUND   2 (verifier looks for `const LEVELS = [...]` in
                           `index.html` but the level data is in
                           `levels_inline.json` — historical tool defect.)
TOOL:EMPTY_LEVELS   1   (Found 0 levels — same root cause.)
```

### 1.1 — 9 exit-1 / broken-tool games (P0 for R2)

| slug | verifier path | defect | evidence tail |
|---|---|---|---|
| anagram | `verify_independent.js` | `TOOL:LEVELS_NOT_FOUND` | `❌ ERROR: No LEVELS array found!` |
| compass | `verify_levels.js` | `EXIT=1` | "FAIL" rows in output |
| crankshaft-linkage | `verify_engine.js` | `TOOL:LEVELS_NOT_FOUND` | (no LEVELS extracted) |
| kurodoko | `verify_independent.js` | `EXIT=1` | (FAIL row) |
| nurimeizu | `verify_engine.js` | `EXIT=1` | (FAIL row) |
| nuritwin | `verify_engine.js` | `EXIT=1` | (FAIL row) |
| pin-pull-puzzle | `verify_engine.js` | `TOOL:EMPTY_LEVELS` | `Found 0 levels` |
| stostone | `verify_independent.js` | `EXIT=1` | (FAIL row) |
| twodoku | `verify_independent.js` | `EXIT=1` | (FAIL row) |
| (doppelblock) | `verify_engine.js` | `TOOL:LEVELS_NOT_FOUND` | (also history: pre-fd4724a8) |
| (akichiwake) | `verify_engine.js` | `TIMEOUT_RUN` | (90s cap, likely real) |

These are NOT confirmed gameplay defects. Each is "verifier says FAIL or can't
parse levels". R2 must re-instrument each verifier to (a) read `levels_inline.json`
when present, (b) classify its own FAIL message as "level really unsolvable" vs
"verifier bug", and (c) only then escalate to a real gameplay repair.

### 1.2 — 1 game with confirmed unsolvable levels

| slug | verifier | parsed pass / fail | unsolvable | comment |
|---|---|---|---|---|
| color-pour | `verify_engine.js` | 20 / 30 | **11** | R2 must regenerate or fix the 11 failing levels; the `verify_engine.js` itself runs to completion. |

### 1.3 — 37 verifiers confirmed pass with parseable count

These are the only verifiers whose output the runner can structurally trust as
"all levels are unique-solution solvable at their declared par". Examples:
sokoban-switch 30/30, snake-pit 30/30, futoshiki 30/30 unique, shikaku, kakuro,
aquarium, cave, etc. The rest (99/149) ran clean (exit 0) but didn't print a
N/M line the runner could parse — they probably exit 0 with a freeform "all
verified" message. R2 should upgrade the runner to also accept the success
keywords (`UNIQUE`, `VERIFIED`, `ok`, etc.).

## 2. Duplicate-mecanic cluster (P0 for R2)

Cluster key = first tag from the puzzle/logic/sudoku/nikoli/sort/merge/word/card/arcade/board/racing/casual family, else last name token. Game-name prefix clustering produced 0 ≥3-matches (catalog has been mostly de-duped already since cleanup phase 1+2). Family-tag clustering is the right lens for the residual overlap.

| size | cluster | slugs (full list elided; example) |
|---|---|---|
| 169 | **puzzle** | pin-pull-puzzle, pattern-matrix, hop-warp, tap-away, fulcrum-balance, circuit-logic, labyrinth, 2048, color-sort, sudoku, minesweeper, … (most are click-grid Nikoli/nonograms/number-fill — at least 60 are Nikoli family which have very similar UI) |
| 58 | **logic** | sokoban, logic-gates, kenken, kakuro, mastermind, hashiwakero, bloxorz, tens-game, crossmath, cryptograms, who-is, two-dots, … (sokoban variants + Latin-square-style puzzles) |
| 41 | arcade | fruit-slash, basketball-shoot, neon-run, pong, ball-catch, … |
| 19 | sudoku | killer-sudoku, blockudoku, color-sudoku, thermo, X, odd-even, sandwich, arrow, hyper, consecutive, girandola, jigsaw, outside, palindrome, marginal, little-killer, samurai, butterfly, woodoku |
| 16 | word | word-puzzle, crossword, compound-word, quordle, contexto, waffle, wordle, word-scramble, words-klondike, text-twist, word-card-sort, spelling-bee, word-ladder, strands, four-pics-one-word, knotwords |
| 14 | nikoli | tentai-show, hotaru-beam, country-road, str8ts, aquarium, cave, herugolf, stostone, dominosa, kropki-sudoku, tents-and-trees, kurodoko, gokigen-naname, tetravex |
| 13 | card | memory-match, solitaire, freecell, hearts, pyramid-solitaire, spades, tripeaks, golf-solitaire, math-24, go-fish, tripeaks-solitaire, solitaire-roguelite, stacklands |
| 11 | merge | kitty-cafe, merge-kingdom, threes, find-n-merge, merge-sweets, coin-machine, duck-merge, veggie-merge, emoji-merge, atomas, hexa-2048 |
| 9 | board | chess, checkers, go, battleship, mancala, yahtzee, farkle, cribbage, hexxagon |
| 4 | sort | sushi-stack, nut-sort, wool-sort, pocket-sort |
| 4 | racing | moto-x3m, duck-life, type-racer, monster-truck-madness |

**Initial analysis (R2 must verify with rule-reads, not just name/heuristic):**
- **sudoku-19**: 19 sudoku variants — most add one orthogonal constraint (Killer cages, thermo arrows, X diagonals, anti-knight, anti-king, odd-even parity, sandwich sums, etc.) and are well-recognized as different puzzles. NO deletion justified on rules alone.
- **word-16**: 5 of these are 6-letter guessers (wordle, quordle, waffle, 4-pics-1-word, context), but the rest are anagrams / word-climb / crosswords — different games. R2 must read each rule.
- **nikoli-14**: the canonical Nikoli family — almost all are different rules. No deletion.
- **merge-11**: 6 of 11 are "merge 2 to make 1 bigger item" clones (watermelon-merge, duck-merge, kitty-cafe, merge-sweets, etc.). R2 should evaluate keeping 1-2 keepers.
- **puzzle-169** is a catch-all bucket; de-duping within it requires reading the IIFE state-machine function name in each `index.html` (R2 step).

## 3. Quality-score (R1 placeholder)

The 6-dim quality scoring (playability 30 / input 15 / UX 10 / visual 10 /
lifecycle 10 / persistence 10 / verifier 15) was the third Codex goal. It was
not completed in R1 because Codex's single attempt was consumed by the level
solvability sweep + the duplicate-mecanic clustering. The audit_runner already
records raw static evidence per game (index.html, levels*.json, verify_*);
R2 should compute the 6-dim score from that static snapshot — no new toolchain
needed.

## 4. Recommended R2 priorities (next concrete step)

1. **Fix `color-pour` 11 unsolvable levels** (highest player impact, smallest scope).
2. **Repair the 9 broken verifiers** so the next sweep can re-instrument them; in particular make the runner `read levels_inline.json` when `LEVELS = [` is missing in `index.html`. This upgrades the 91% → ~97% trust on the verifier surface.
3. **Per-cluster rule-read + quality score** for the 12 clusters in section 2, starting with `merge-11` (likely 4-5 redundant clones) and `racing-4` (moto-x3m / duck-life / type-racer are NOT actually racing-clones — type-racer is a typing game, the label is wrong).
4. **Update the production cron `36e21d25f954`** to ingest this R1 report as the "first sweep" baseline and resume with the next 5 games from the highest P0-priority list.

## 5. Files written by R1

- `/home/msdn/gamezipper.com/.audit/audit_runner.py`        — the verifier-sweep runner
- `/home/msdn/gamezipper.com/.audit/verifier_runs.jsonl`     — 149 records (one per verifier)
- `/home/msdn/gamezipper.com/.audit/audit_summary.json`     — aggregated counts
- `/home/msdn/gamezipper.com/.audit/r1-2026-07-19.md`        — this report

Nothing was committed to git; nothing pushed. Production HEAD is still
`fd4724a8`. The cron `36e21d25f954` (next run 2026-07-19 23:20 CST) is now
seeded with this baseline.

## 6. Honest limitations of R1

- The 9 "exit-1" verifiers were NOT opened and read individually; they are
  likely "verifier says FAIL" but the body could be either "verifier is buggy"
  or "the level really is unsolvable". R2 must classify them with
  body-content grep.
- The 4 puzzle-categories with biggest overlap (sudoku-19, nikoli-14, word-16,
  puzzle-169) were clustered by tag, not by rule-read. They are not
  deletions — just candidate audit groups.
- The 6-dim quality score was not computed for any game. The static
  evidence is now on disk (verifier_runs.jsonl + index.html paths) so R2
  only needs a scoring script, not more sweeps.
- Codex single attempt was wasted on a `Reconnecting 1/5` network idle, not
  on real work. Hermes's local audit pass covered the verifier sweep + the
  duplicate-mecanic cluster but not the quality score.
