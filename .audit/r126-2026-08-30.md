# QA Sweep 126 (2026-08-30) — Per-game deep verification batch

**Scope**: 5 unverified live games (odd-one-out, prism-path, pocket-sort, usotatami, monster-truck-madness) — picked by BI 7d PV (5/5/4/4/3) and category variety (4 puzzle + 1 racing).

**Method**: per-game `verify_engine.js` (real input replay) + Kachilu live `qv126-*` sessions + click-flow probe (canvas-drawn menu / splash → levelselect → game) + localStorage inspection.

**Verdict**: **5/5 PASS**, 0 P0-P3 defects, 0 code changes shipped.

**Coverage delta**: 298 → **303/453** (+5 strict-PASS entries).
**Zero-issue sweeps**: 2 → **3** (3rd consecutive sweep with 0 fixes).
**Converged**: true (zero_issue_sweeps >= 2 and no catalog-head drift).

## Per-game evidence

### odd-one-out (puzzle, PV=5)
- **Verifier**: `node odd-one-out/verify_engine.js` → exit 0; 20/20 PASS
  - real taps + timer/hint/lock paths exercised
  - noted historical P1 fixes: pizza emoji alt identical (unwinnable emoji levels), P1 progress never saved on completion, P2 final level stars never recorded — all P1/P2 already addressed
- **Browser (Kachilu)**: `qv126-odd-one-out` — canvas `#c` 540×577 with **311580 non-zero RGB** on load; footer 8 links, ad-div, monetag, h1 present, no error toast
- **Click path**: R329 menu-gated with `▶ PLAY` drawn on canvas at `bx=W/2-90, by=H*.55, bw=180, bh=52` (confirmed via source grep line 595-599)
- **Site-chrome**: all PASS
- **Verdict**: PASS

### prism-path (puzzle, PV=5)
- **Verifier**: `node prism-path/verify_engine.js` → exit 0; 30/30 PASS
  - embedded solution replay through real canvas pointerdown taps; L1 won in 7 taps (5ms), L30 won in 41 taps (20ms)
- **Browser (Kachilu)**: `qv126-prism-path` — splash→levelselect→game state transitions confirmed:
  - click `#btn-start` → splash.display:none, levelselect.display:flex
  - first non-locked `.lvl-btn` (lvlCount=6 = 6 levels per tier) → levelselect.display:none + `state='game'`
  - canvas `#board` 1280×577 with **76348 non-zero RGB** post-click
- **Persistence**: localStorage key=`prism_path_save_v1` registered (only persists on `onWin()` / tier change — expected null after fresh play)
- **Verdict**: PASS

### pocket-sort (puzzle, PV=4)
- **Verifier**: `node pocket-sort/verify_engine.js` → exit 0; 30/30 PASS
- **Browser (Kachilu)**: `qv126-pocket-sort` — canvas `#gameCanvas` 448×582 with **260736 non-zero RGB** (game running on load); full UI: btnMenu (☰) + btnUndo (↶3) + btnShuffle (♻1) + btnClear (✕1) + btnReveal (◉1) + btnRestart + btnLevels; footer 7 links, ad-div, monetag, h1 OK
- **Verdict**: PASS

### usotatami (puzzle, PV=4)
- **Verifier**: `node usotatami/verify_engine.js` → exit 0; 30/30 PASS
  - all 30 levels Expert 7×7 grid; engine `checkWin()` confirmed for all
- **Browser (Kachilu)**: `qv126-usotatami` — canvas `#game` 348×348 with **121104 non-zero RGB** on load; full UI: btnMenu (📋 Levels) + btnHint (💡 Hint (3)) + btnCheck (✓ Check) + btnClear (↺ Restart) + btnSettings (⚙ Settings)
- **Click path**: ↺ Restart click → canvas expanded to 368×368 with **135424 non-zero RGB** (level reset successful)
- **Verdict**: PASS

### monster-truck-madness (racing, PV=3) — only non-puzzle in batch
- **Verifier**: `node monster-truck-madness/verify_engine.js` → exit 0; 6/6 PASS
  - throttle drive to finish flag; 2 wins, 0 crashes, maxX=1709, finish=1930
- **Browser (Kachilu)**: `qv126-monster-truck-madness` — canvas `#c` 954×537 with **512298 non-zero RGB** (game already running); footer 8 links, ad-div, monetag, h1 OK
- **Resource note**: `/icon.png` briefly 503 in browser (CDN transient — HTTP 200 confirmed via curl, last-modified 2026-08-30 00:10:30 GMT, not a game bug)
- **Verdict**: PASS

## State file

- Path: `/home/junze/.hermes/state/gz-qa-convergence.json` (atomic write via tempfile + os.replace)
- `current_sweep`: 125 → **126**
- `coverage`: 298/453 → **303/453** (+5 new strict-PASS entries)
- `zero_issue_sweeps`: 2 → **3**
- `converged`: true (no catalog-head drift, zero_issue_sweeps >= 2)
- `catalog_head`: `a8208e6996` → `a9b9d092e1` (current main; watchdog R556 deploy `5bd9e2c6a2` + R557 deploy `6912b10f69` + monetag-manager fix `a9b9d092e1` all integrated)
- `last_batch`: 5/5 (odd-one-out, prism-path, pocket-sort, usotatami, monster-truck-madness)
- `fixes_applied`: 0 (sweep had zero defects)

## Test library / catalog state

- test-library: **v1.235.0** (R352 — 8 new test cases from Chrome 152 STABLE deployment + 2-week cadence change)
- games-data.js GAMES array: **453 live games** (no change this sweep)
- No commit/push needed (0 fixes; 5/5 PASS)

## Sweep cadence notes

- Coverage: **303/453 = 66.9%** (was 65.8% sweep 125)
- Unverified: **150 live games** remaining (was 155; 5 added this sweep)
  - PV>=2 candidates: 27 (sorted by PV: odd-one-out / prism-path / sling-smash / pocket-sort / usotatami / zip-tie / gecko-out / hex-path-spin / magic-square / monster-truck-madness / penalty-shootout / recoil / samurai-sudoku / snake-pit / soap-slice / sticker-book-puzzle / domino-toppler / grow-worm / jigpic-solitaire / numbrix / outside-sudoku / petal-path / stained-glass / str8ts / sukoro / valve-network / wood-turning)
  - After this sweep PV>=2 missing: 22 (sling-smash 5 + zip-tie 4 + gecko-out 3 + hex-path-spin 3 + magic-square 3 + penalty-shootout 3 + recoil 3 + samurai-sudoku 3 + snake-pit 3 + soap-slice 3 + sticker-book-puzzle 3 + domino-toppler 2 + grow-worm 2 + jigpic-solitaire 2 + numbrix 2 + outside-sudoku 2 + petal-path 2 + stained-glass 2 + str8ts 2 + sukoro 2 + valve-network 2 + wood-turning 2 = 22)
  - 75 zero-PV (most pipeline-introduced with no BI traffic yet)
- Next sweep 127 will pick 5 more from this PV-priority missing list (sweep 126 batch covers top 4 puzzle + 1 racing; next pick should bias toward arcade/board/casual categories for archetype variety)
- 3 zero-issue sweeps: catalog structurally stable. Convergence countdown active.

## BI signal (7d, sanity check)

- Top dead_click pages: /cryptograms/ 12, /save-the-doge/ 10, /balance-scale/ 3, /cloud-sheep/ 3, /embed-games.html 3 — R556 already addressed top 2; balance-scale and cloud-sheep are R556-next candidates
- Top inp_worst (high-INP) pages: / 172, /bus-traffic-fever/ 48, /2048/ 40, /embed-games.html 33 — all R526/R551/R552 already addressed
- No new P0 signals for sweep 126 candidates

## Watchdog / catalog-head integration

- HEAD `a8208e6996` (sweep 125) → `a9b9d092e1` (current main)
  - R556 `5bd9e2c6a2` (cryptograms + save-the-doge dead_click fixes)
  - R557 `6912b10f69` (game pipeline candidates)
  - **NEW**: `a9b9d092e1` (monetag-manager inject network='adsense' on banner_no_fill Tier 1 failure)
- `_catalog_head_drift`: false (state updated atomically with current HEAD)
- All sweep 126 game entries stamped with `catalog_head: a9b9d092e1`

## MEMORY (sweep 126)

- **Kachilu CLI `viewport` command is NOT available in current version** (verified — `Unknown command: viewport`); the skill mentions it but the installed version doesn't expose it. Mobile-viewport QA must use eval-based viewport emulation OR external playwright. For sweep-tier desktop QA, default 1280×577 is sufficient; sweep 126 skipped mobile-viewport secondary checks (game architecture + verifier DOM checks already cover rendering correctness at desktop size)
- **prism-path click-flow lesson**: canvas-drawn splash menus are confirmed via `getComputedStyle(splash).display === 'none'` AFTER click, not via canvas pixel delta. Splash overlays don't write to canvas; they remove themselves. Verify state transition by reading splash+levelselect display values
- **state._saveMem introspection works for IIFE-scoped save objects**: prism-path's `save` is IIFE-scoped but exposed via `typeof save !== 'undefined'` from inside an IIFE-wrapped eval. Reading `save.unlocked`/`save.stars` confirms save lifecycle without localStorage. If `localStorage.getItem(KEY)` is null but `save` has expected default fields → game is in fresh-session state (expected when no win yet)
- **oddone-out canvas-drawn PLAY coords**: `bx=W/2-90, by=H*.55, bw=180, bh=52` at canvas internal coords (540×577). For click probes, multiply by `rect.width/cv.width` scale factor (here 1.0 since canvas attribute = display size)
- **monster-truck-madness /icon.png 503 is CDN transient**: the icon file is HTTP 200 from CDN (`last-modified: Sun, 30 Aug 2026 00:10:30 GMT`); the 503 in the browser's resource timing is a one-time cache miss. Not a game bug — ignore unless persistent across multiple sessions
- **State-update atomic-write pattern verified**: `/tmp/sweep126-update.py` uses tempfile + `os.replace()` (POSIX-atomic). State file at `/home/junze/.hermes/state/gz-qa-convergence.json` updated cleanly to sweep 126 with current HEAD `a9b9d092e1`