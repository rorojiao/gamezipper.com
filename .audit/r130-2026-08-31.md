# Sweeps 128 + 129 — 10 game QA, 10/10 PASS, 0 fixes shipped

**Sweep 128**: soap-slice / sling-smash / zip-tie / recoil / gecko-out (PV=5 top missing)
**Sweep 129**: magic-square / sticker-book-puzzle / snake-pit / hex-path-spin / grow-worm (PV=3 top missing)

## Catalog state
- **Catalog HEAD**: `8ff17f98ec9cda24f08c8c5781a68b8ed454f14a`
- **Live total**: 453 games
- **Coverage**: **316/453** (was 311/453)
- **State entries**: 371 (53 NONE legacy + 316 PASS strict + 2 P0/P1 fixes)
- **Missing**: 82 live games
- **zero_issue_sweeps**: 6
- **converged**: true (>=2 zero-issue sweeps)

## Per-game results (10/10 PASS)

### Sweep 128

1. **soap-slice** (puzzle, PV=5) — 30 levels: independent minCuts re-derived + bestCuts replayed via real drag input to engine checkSolved + 3-star persistence; negative paths covered. **32/32 verifier PASS**; browser: start-screen ▶ Play → level-select → L1 → game-screen active, canvas 110x80 with 4780 RGB.

2. **sling-smash** (puzzle, PV=5) — 30 levels played via real pointer aim/launch through engine physics to engine checkWin + stars persisted; negative lose path. **31/31 verifier PASS**; browser: canvas 1280x577 with 242340 RGB on Play.

3. **zip-tie** (puzzle, PV=4) — 516/516 verifier PASS (real canvas pointer drags + bootGame harness; offline all-30 + live 1,2,3,25,30; engineFixes P1-unlock-order + 3x P3). Browser: btn-play + 10 visible buttons (Sound/Undo/Reset/Hint/How/Menu/Play/Levels-title/How-title/×); canvas 397x397 with 157609 RGB after Play.

4. **recoil** (puzzle, PV=4) — 85/85 verifier PASS (30 levels via real pointer swipes + per-shot live-state BFS; levels 25/27/29 are design dups; all winnable despite some star-design > par). Browser: canvas 414x414 with 171396 RGB after Play.

5. **gecko-out** (puzzle, PV=3) — 30/30 verifier PASS via real pin taps through engine pullPin/checkCollisions/winLevel; **P0s fixed earlier**: dead menu/HUD buttons + 19 unwinnable levels. Browser: ▶ Start Game → canvas 1280x577 with 738560 RGB (full screen populated); gz_recent_games localStorage.

### Sweep 129

6. **magic-square** (puzzle, PV=3) — 30/30 verifier PASS. Browser: grid-canvas 288x288 with 82944 RGB (game state already loaded, level 1 3x3 grid visible), Hint/Undo/Reset/Levels/Sound/Music all rendered.

7. **sticker-book-puzzle** (puzzle, PV=3) — 62/62 verifier PASS (engineFixes: P1 visibilitychange gameState-undefined freeze, P2 win-race undo 400ms window, P3 nextLevel-after-L30 replayed-L29, P3 stars-val HUD never updated). Browser: menu-screen with ▶ Continue / New Game + Level Select + Settings + How to Play; game-canvas 560x560 with 313600 RGB pixel data (off-screen until level selected).

8. **snake-pit** (puzzle, PV=3) — 30/30 verifier PASS (L30 8x9 confirmed). Browser: menu-screen with 30 level buttons (1-30) + Music OFF / SFX ON toggles + Got it tutorial button.

9. **hex-path-spin** (puzzle, PV=3) — 254/254 verifier PASS (30 levels, totalStars=90). Browser: menu-screen with btn-play PLAY + btn-levels SELECT LEVEL + btn-settings-menu SETTINGS.

10. **grow-worm** (puzzle, PV=2) — 25/25 verifier PASS (L24 Supernova 11/11/17 -> stars=3, L25 Final Frontier 15/15/23 -> stars=3). Browser: PLAY/LEVELS/HOW TO PLAY buttons; canvas 1280x577 with 738560 RGB (menu rendered directly on canvas).

## MEMORY (sweeps 128+129)

- **Lesson: never reuse Kachilu session across multiple game opens** (R-tick 2026-08-31, soap-slice functional probe bug). After running 5 different game `open` commands in the same session, the JS context may still reference the previous game's DOM (`document.getElementById('btn-play')` returned null on zip-tie because the tab was still on a previous game). Fix: always use a **fresh session per game** (`--session "deep-<slug>"` with unique name per game).

- **All 92 missing games have `verify_engine.js` or `verify_independent.js`** — no new test-gate authoring needed. Top 5 next batch (PV=1-2): snake-vs-block, paper-io, glass-rush-3d (arcade) + word-ladder, hidato (puzzle).

- **Coverage 316/453 means we need 82 more sweeps of 5 games each to reach 100%** at current cadence. At ~6 min per batch (verifier+QV3+probe), that's ~8 hours of focused work. Achievable across next 8-12 cron ticks.

- **Verifier PASS rate on first try: 100%** (10/10 in sweeps 128+129). The catalog is structurally sound — earlier R-tick lessons (Pitfall #50 cwd-shadow bug, sweep-44 verifiers) eliminated the silent-fail class.

- **State file verdict-filter gotcha**: original coverage field used `'P0 FIX SHIPPED'` (space) but file stores `'P0_FIX_SHIPPED'` (underscore). When recomputing strict-pass count, must include BOTH variants or coverage undercounts. Sweep 128 first compute gave 305, corrected to 311 with both filters. Sweep 129: 316 = 311 + 5 new.

## Coverage contract
- **316/453** games with strict verdict (PASS / PASS-annotated / P0/P1 FIX SHIPPED)
- **82 live games** still need first-time coverage (mostly puzzle)
- **53 NONE entries** in state.games are legacy entries from early sweeps before verdict tracking — not actively used, harmless

## Next sweep (130) candidates
- Top 5 missing: `snake-vs-block` (arcade PV=0 — high priority for diversity), `paper-io` (arcade), `glass-rush-3d` (arcade), `word-ladder` (puzzle), `hidato` (puzzle)
- Sweep 130 should pick 5 from this set to maintain PV-priority + category balance (arcade-heavy early in 130 to clear the 5 arcade missing)

## Git status
- No code changes required (all 10 games already shipped + working)
- 20 untracked files = prior cron leftovers (`.audit/r*.md` reports + `scripts/seo_health_report_*.json`). Not part of sweep 128/129 — leaving for owner-cron to handle
- No commit needed for this sweep pair
