Build a complete, commercial-quality single-file HTML5 Canvas game called "Nondango" at /home/msdn/gamezipper.com/nondango/index.html.

STEP 1: Read these two files for the spec and data:
- /home/msdn/gamezipper.com/nondango/BENCHMARK.md  (rules + design)
- /home/msdn/gamezipper.com/nondango/levels.json   (27 levels — EMBED THIS DATA INLINE in the HTML as a JS const, do NOT fetch it)

GAME: Nondango — a Nikoli logic puzzle. The level data format per level:
  {id, tier, w, h, grid, regions, givens, solution} where
  - grid[r][c]:    1 = cell has a CIRCLE, 0 = EMPTY cell (draw nothing, no circle)
  - regions[r][c]: region id (cells with same id form a region; draw thick borders BETWEEN cells of different regions)
  - givens[r][c]:  1 = pre-filled BLACK circle (locked), 0 = pre-filled WHITE circle (locked), -1 = uncolored circle (player fills). Empty cells = -1.
  - solution[r][c]: 1=black, 0=white (for circle cells), -1=empty. Use for hints/check only.

RULES the player must satisfy to win:
  1. Each region has EXACTLY ONE black circle.
  2. No three CONSECUTIVE same-colored circles in any horizontal, vertical, OR diagonal line (all 8 directions). An EMPTY cell or a different-colored circle breaks the run.

PLAYER INTERACTION:
  - Render the grid: each cell with a circle (grid==1) shows a circle. Empty cells (grid==0) show nothing.
  - Draw thick colored borders between cells of different regions (region boundaries are the puzzle's key visual).
  - Pre-filled circles (givens 0/1) are locked: black filled, white outlined — not clickable.
  - Uncolored circles (givens==-1) start as a faint grey ring. Click/tap to cycle: grey(uncolored) → black(filled) → white(outlined) → grey.
  - Touch support: touch-action:none on canvas, pointer events, min 44px tap targets.
  - Win detection: when all uncolored circles are assigned AND both rules are satisfied → win (confetti + celebratory sound + timer stops). Use a correct independent checker (same logic as the rules above).
  - Check button: highlight violations (3-in-a-line runs in red; regions with wrong black count outlined).
  - Hint button: reveal one correct uncolored circle (from solution) — costs a hint counter.
  - Undo button: step back through the player's moves.
  - Reset button: clear all player-placed circles back to uncolored.
  - Level selector: 27 levels across 6 tiers (Beginner/Easy/Medium/Hard/Expert/Master). Show tier labels, allow prev/next + a level grid picker. Track completion + best time per level in localStorage (include a version field, e.g. "nondango_v1").
  - Timer per level. Tutorial overlay explaining rules (skippable, shown first time).
  - Mute toggle for audio.

TECHNICAL REQUIREMENTS (MANDATORY — these are QA-checked):
  - SINGLE self-contained file: nondango/index.html. No external deps except CDN fonts. Embed levels.json data inline as a JS const.
  - Canvas-based rendering, smooth 60fps, delta-time game loop (frame-rate independent).
  - touch-action:none on the canvas; overflow-x:hidden AND user-select:none on body.
  - EXACTLY ONE analytics tag: <script src='https://site-analytics.cap.1ktower.com/hit?s=gamezipper.com&p=nondango'></script>
  - JSON-LD structured data: include VideoGame + FAQPage + HowTo + BreadcrumbList (4 separate <script type="application/ld+json"> blocks).
  - og:title, og:description, og:image meta tags; canonical link https://gamezipper.com/nondango/
  - <title>Play Nondango Online Free - Binary Circle Logic Puzzle | GameZipper</title>
  - Procedural BGM + SFX via Web Audio API (OscillatorNode/GainNode): click sound, place-black, place-white, error, win fanfare, button hover/click. A gentle ambient loop BGM. Mute toggle persisted.
  - Single cleanup() entry point that cancels all rAF, timers, and audio nodes, removes listeners (no memory leaks). Duplicate-click prevention.
  - Particles on placement, screen feedback, smooth entry/feedback animations.

VISUAL STYLE:
  - Dark gradient background (deep navy/purple), neon accent colors. GameZipper brand style.
  - Rounded corners, subtle shadows, glass-morphism panels. text-shadow for titles (NEVER -webkit-text-stroke).
  - Region borders in a distinct neon color. Black circles: filled dark with subtle glow. White circles: bright outlined. Uncolored: faint grey ring.
  - Emoji-free title. ALL English UI, NO Chinese text anywhere.
  - Responsive: works on desktop (1280x720) and mobile (390x844). Auto-scale the canvas/grid to fit.

QUALITY:
  - All 27 levels must be playable and winnable (the data is pre-verified unique).
  - The win checker must exactly implement the two rules.
  - No JS errors on load or during play.

After writing the file, verify it: ensure it has </html>, is >=25KB, and the levels data is embedded. Output the final file size.
