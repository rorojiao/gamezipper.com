# GameZipper 100% Master Test Case Library
**Version**: 1.174.0
**Effective Date**: 2026-07-12 (v1.174.0 auto-research, 4h cycle 53)
**Build**: R291 (Dynamic Test Intelligence 4h cron evolution)

**Version Note**: v1.174.0 = R291 Dynamic Test Intelligence cron — 7 new test cases from 2026-07-12 industry research (4h cadence cycle 53): Edge 2026 Copilot UI overhaul — Microsoft Edge redesigns UI to match Copilot design language (increased border-radius, capsule-shaped controls, new toggle switches) and progressively removes unique features (Outlook sidebar) to align with Chromium, auto-opening Copilot on new tabs via experiments W-329 (sohu.com/a/1008833159_121956424 Apr 13 2026 — "微软Edge浏览器计划在2026年迎来重大UI改版,设计风格将向Copilot靠拢...新版Edge的圆角设计弧度增大...开关控件也采用全新设计,与Copilot的风格高度相似...Edge正逐步移除独有功能,如Outlook邮件侧边栏,并采用Chrome版本的画中画设计...微软将持续通过实验,推动新标签页自动打开Copilot" — the BROWSER-UI-CONVERGENCE-AND-AI-PANEL-AUTOOPEN dimension where Edge's UI is converging with Chrome via Chromium and simultaneously pushing the Copilot AI panel into the new-tab flow — game pages rendered inside Edge may have their CSS `border-radius`, toggle switches, and `picture-in-picture` element styling change unexpectedly due to Edge's new Copilot-flavored design system); Safari 26 shipping fingerprinting protection for known tracking scripts — `134227067` shipped in Safari 26 (unlike `134226694` tracking-param stripping which was removed) blocks known fingerprinting scripts S-396 (cloud.tencent.com/developer/article/2637644 Jul 12 2026 — "Safari版本26已经发布,版本计划是包含两个保护隐私功能:Added support for preventing fingerprinting for known tracking scripts. (134227067)...第一个134226694...但实际发布的版本26里将134226694移除...第二个134227067 [fingerprinting prevention] remains" — the FINGERPRINTING-SCRIPT-BLOCKLIST dimension where Safari 26 actively blocks a list of known fingerprinting scripts — games that include analytics/ad scripts on Safari's blocklist may have those scripts silently fail to load, breaking functionality like Canvas fingerprinting-based analytics, FLoC replacement APIs, or device-info collection); HTML-in-Canvas API (WICG/Chromium) — new Chromium API that unifies DOM+CSS layout with Canvas/WebGL GPU pipeline, eliminating the either/or rendering choice W-330 (sohu.com/a/1028944399_122066678 May 28 2026 — "Chromium项目近日宣布推出全新的HTML-in-Canvas API,旨在解决前端渲染领域长期存在的核心割裂问题...该API由Web平台孵化器社区组(WICG)提出,将彻底改变现有的DOM+CSS与Canvas/WebGL二选一的困境...DOM+CSS具备完善的布局、文本排版及无障碍能力,但无法接入GPU管线" — the CANVAS-DOM-UNIFIED-RENDERING-API dimension where a new API allows DOM content to be rendered into a Canvas context with GPU acceleration — games that detect rendering mode via feature checks (`typeof HTMLCanvasElement`, `getContext('2d')` presence) may misidentify the environment when HTML-in-Canvas is active, and screenshot/share features relying on `canvas.toDataURL()` now capture DOM content that was previously invisible to canvas reads); Chrome WebGPU 2.0 + V8 engine upgrade — Chrome 2026 ships WebGPU 2.0 and new V8 engine claiming 30% faster page loads, 25% lower memory, 100-tab smoothness P-152 (ppzy.com/app/49612.html May 11 2026 — "Chrome2026版...全新V8引擎与 WebGPU2.0加持,页面加载提速30%,内存占用降低25%,百标签并发仍流畅稳定" — the NEXT-GEN-GPU-API-AND-ENGINE-OVERHAUL dimension where WebGPU 2.0 replaces/supersedes WebGL for new content and the V8 overhaul changes JIT/timing behavior — WebGL games may run slower if Chrome de-prioritizes WebGL in favor of WebGPU, and timing-sensitive game loops may behave differently under the new V8 JIT); Chrome one-time camera/microphone permissions + one-click unsubscribe notifications — Chrome 2026 adds single-use media permissions and notification unsubscribe, changing how `getUserMedia()` prompts work S-397 (ppzy.com/app/49612.html May 11 2026 — "安全方面,后台自动安全检查、单次相机/麦克风权限、一键退订网站通知等功能,为用户提供更可靠的隐私保护" — the SINGLE-USE-MEDIA-PERMISSION dimension where Chrome now offers one-time camera/mic access that expires after the page session — games using `getUserMedia()` for AR/voice features will lose their permission grant on reload/navigation, requiring re-prompting, and games that assume persistent media permission will silently fail on second launch); Chrome battery-saver tab freezing — new Edge/Chrome power mode freezes idle tabs to reduce energy, distinct from Memory Saver (memory) — this targets CPU/energy P-153 (ppzy.com/app/49612.html May 11 2026 — "性能上,新增的省电模式通过冻结闲置标签有效降低能耗" — the ENERGY-SAVER-TAB-FREEZE dimension where a dedicated power-saving mode freezes background tabs' JS execution and timers to save battery — `setInterval`/`setTimeout` in frozen game tabs are paused indefinitely, `requestAnimationFrame` stops, and game state advances incorrectly when the tab is unfrozen (e.g., a game using wall-clock time `Date.now()` for scoring will compute a huge time delta on unfreeze, breaking leaderboards/timers)); Firefox 150 Mythos AI 180 high-severity bugs confirmed by Mozilla — Mozilla confirms 180 of the 271 AI-found Firefox bugs are high-severity, triggerable by normal browsing S-398 (so.html5.qq.com/page/real/search_news?docid=70000021_53769fd53ba77252 May 8 2026 — "Mozilla 团队借助 Anthropic 的 Mythos Preview AI 模型,在火狐 Firefox 浏览器 150 版本中,发现并修复了 271 个漏洞...在发现的 271 个漏洞中,有 180 个达到"高危"级别,意味着用户正常浏览网页就可能触发" — the AI-DISCOVERED-BROWSER-VULNERABILITY-AT-SCALE dimension building on prior Firefox coverage but now with CONFIRMED severity breakdown — 180 high-severity bugs in Firefox 150 means any game page could be a trigger vector, and the scale demonstrates that AI bug-hunting is finding production-grade vulnerabilities that human auditing missed, raising the baseline expectation that ALL browsers have a large undetected-bug backlog)

**Review Cycle**: Every 4 hours (auto-update via Dynamic Test Intelligence)

---
---

## New Test Cases Added in v1.174.0 (R291 — July 12 2026, 4h cycle 53)

### Web Platform / Browser (W)

- [W-329] **[P2]** Edge 2026 Copilot UI Overhaul — Browser UI Convergence + AI Panel Auto-Open Changes Game Page Rendering (sohu.com/a/1008833159_121956424 Apr 13 2026 — "微软Edge浏览器计划在2026年迎来重大UI改版,设计风格将向Copilot靠拢｡新版Edge的圆角设计弧度增大,视觉效果更柔和､饱满,与Copilot设计语言保持一致｡开关控件也采用全新设计,与Copilot的风格高度相似...Edge正逐步移除独有功能,如Outlook邮件侧边栏,并采用Chrome版本的画中画设计...微软将持续通过实验,推动新标签页自动打开Copilot"). **Why this is the BROWSER-UI-CONVERGENCE-AND-AI-PANEL-AUTOOPEN dimension distinct from W-328 (Safari tracking param policy reversal) and S-395 (Chrome Mariner auto-browse agent)**: W-328 is URL param behavior; S-395 is an AI agent browsing autonomously; W-329 captures the EDGE-UI-REDESIGN class where Microsoft Edge's 2026 visual overhaul (larger border-radius, capsule controls, new toggle switches matching Copilot design language) directly affects how game page CSS is rendered — Edge's user-agent stylesheets for `border-radius`, form controls (`<input type="checkbox">`, `<button>`), and `picture-in-picture` video elements are changing, which can break pixel-perfect game UI layouts. **Why this matters for GameZipper**: (a) game UIs with custom-styled checkboxes/toggles may inherit Edge's new Copilot-flavored styling unless explicitly overridden, causing visual regression; (b) the auto-open-Copilot-on-new-tab experiment means users opening a new tab to navigate to a GameZipper game may have the Copilot panel consume screen real estate, pushing the game viewport off-screen or resizing the window; (c) Edge removing the Outlook sidebar and adopting Chrome's picture-in-picture means `<video>` PiP behavior changes — games with video tutorials/cutscenes may lose PiP controls or gain unexpected ones; (d) the Chromium alignment means Edge is becoming a Chrome clone, so Edge-specific bugs (if any GameZipper code has Edge workarounds) may surface; (e) the auto-open Copilot injects an AI panel that may intercept keyboard shortcuts or consume CPU, degrading game performance. **QA methodology**: (1) test all game pages in Edge 2026 (stable + beta) — screenshot UI controls (buttons, toggles, sliders) and compare to Chrome — flag any visual delta; (2) test `border-radius` rendering on game UI panels — Edge may apply different default radius; (3) open a new tab, verify whether Copilot auto-opens and whether it affects game viewport dimensions (`window.innerWidth`/`innerHeight`); (4) test `<video>` PiP behavior in Edge vs Chrome; (5) audit game code for `navigator.userAgent` Edge detection that may now be stale; (6) test keyboard shortcuts while Copilot panel is open — verify no interception.

- [W-330] **[P2]** HTML-in-Canvas API (WICG/Chromium) — Unified DOM+Canvas Rendering Changes Feature Detection & Screenshot Capture (sohu.com/a/1028944399_122066678 May 28 2026 — "Chromium项目近日宣布推出全新的HTML-in-Canvas API,旨在解决前端渲染领域长期存在的核心割裂问题｡该API由Web平台孵化器社区组(WICG)提出,将彻底改变现有的DOM+CSS与Canvas/WebGL二选一的困境｡长期以来,开发者在选择前端渲染方案时面临两难:DOM+CSS具备完善的布局､文本排版及无障碍能力,但无法接入GPU管线"). **Why this is the CANVAS-DOM-UNIFIED-RENDERING-API dimension distinct from P-152 (WebGPU 2.0 — new GPU API) and S-393 (ANGLE WebGL heap overflow — security bug)**: P-152 is a new compute/graphics API; S-393 is a security vulnerability; W-330 captures the RENDERING-PATH-MERGER class where the new HTML-in-Canvas API allows DOM/CSS content to be composited INTO a Canvas context with GPU acceleration — this means a `<canvas>` can now contain DOM-rendered text, buttons, and layouts that were previously impossible to put on canvas. **Why this matters for GameZipper**: (a) games that do feature detection like `if (canvas.getContext('2d'))` to decide rendering mode may misidentify the environment when HTML-in-Canvas is active — the canvas reports a 2D context but also contains DOM content; (b) `canvas.toDataURL()` and `canvas.toBlob()` (used by screenshot/share features, high-score capture) will now capture DOM content that was previously invisible to canvas reads — a game's screenshot feature may suddenly include HTML UI overlays, breaking expected screenshot output; (c) games that read canvas pixels for collision detection or OCR (`getImageData`) will encounter DOM-rendered pixels mixed with canvas-drawn pixels, potentially breaking pixel-based game logic; (d) the accessibility story improves (DOM content on canvas is screen-reader accessible) but games relying on canvas being opaque to accessibility tools may behave unexpectedly; (e) performance characteristics change — DOM-to-canvas compositing has different cost than pure canvas drawing. **QA methodology**: (1) detect HTML-in-Canvas support via `typeof HTMLCanvasElement.prototype.transferControlToOffscreen` and new API surface; (2) test all games using `toDataURL()`/`toBlob()` for screenshots — verify output matches expected (no unexpected DOM content); (3) test `getImageData()`-based collision detection games — verify pixel data integrity; (4) audit games for feature-detection code that assumes canvas and DOM are mutually exclusive rendering paths; (5) test on Chrome with HTML-in-Canvas flag enabled vs disabled.

### Security (S)

- [S-396] **[P2]** Safari 26 Fingerprinting Protection for Known Tracking Scripts — Blocklist Silently Fails Analytics/Ad Scripts (cloud.tencent.com/developer/article/2637644 Jul 12 2026 — "Safari版本26已经发布,版本计划是包含两个保护隐私功能:Added support for filtering tracking paramaters in links...Added support for preventing fingerprinting for known tracking scripts. (134227067)...第一个134226694 [tracking param stripping]...但实际发布的版本26里将134226694移除...所以gclid,fbclid的跟踪不受影响[but]第二个134227067 [fingerprinting script prevention] remains in shipping Safari 26"). **Why this is the FINGERPRINTING-SCRIPT-BLOCKLIST dimension distinct from W-328 (Safari tracking-param stripping reversal) and S-391 (Safari CSP bypass CVE)**: W-328 is about URL params (not stripped); S-391 is a CSP bypass vulnerability; S-396 captures the SCRIPT-LEVEL-BLOCKLIST class where Safari 26 actively blocks execution of scripts on a known-fingerprinting blocklist — unlike the tracking-param feature (which was removed), the fingerprinting-script-prevention feature (134227067) SHIPPED in Safari 26 and is actively blocking scripts. **Why this matters for GameZipper**: (a) if any GameZipper analytics script, ad script (Monetag/AdSense), or third-party library is on Safari's fingerprinting blocklist, it will SILENTLY fail to load on Safari 26 — no console error, no network error, the script simply doesn't execute; (b) games that rely on these scripts for functionality (analytics events, ad rendering, A/B testing) will appear broken to Safari 26 users with no visible cause; (c) Canvas fingerprinting (drawing specific text/shapes to canvas and hashing the pixel output to create a device ID) is explicitly targeted — any game or analytics using this technique for fraud detection or user identification will return null/undefined on Safari 26; (d) AudioContext fingerprinting (creating an audio fingerprint via OfflineAudioContext) is also likely blocked; (e) the blocklist is opaque — Apple doesn't publish which scripts are blocked, making diagnosis difficult. **QA methodology**: (1) in Safari 26, load each game page and check Console for blocked-script indicators (Safari shows a "blocked fingerprinting script" warning in some cases); (2) verify analytics beacons fire — check network tab for analytics endpoint requests on Safari 26; (3) test Canvas fingerprinting code — `canvas.toDataURL()` used for fingerprinting should still work (it's a DOM API) but the fingerprinting SCRIPT wrapping it may be blocked; (4) compare ad fill rates on Safari 26 vs Chrome — if Monetag/AdSense scripts are blocked, ad containers will be empty; (5) audit all third-party scripts for fingerprinting-related APIs (canvas.toDataURL + hash, OfflineAudioContext, navigator.plugins enumeration) and test if those scripts load on Safari 26.

- [S-397] **[P2]** Chrome 2026 One-Time Camera/Microphone Permissions — Single-Use Media Grant Expires After Session (ppzy.com/app/49612.html May 11 2026 — "Chrome2026版...安全方面,后台自动安全检查、单次相机/麦克风权限、一键退订网站通知等功能,为用户提供更可靠的隐私保护"). **Why this is the SINGLE-USE-MEDIA-PERMISSION dimension distinct from S-392 (Gemini AI privilege escalation — extension-based) and B-197 (Safari ITP cookie blocking)**: S-392 is extension-based camera/mic hijacking; B-197 is cookies; S-397 captures the SESSION-SCOPED-MEDIA-PERMISSION class where Chrome 2026 offers one-time camera/microphone access that EXPIRES after the page session ends (page reload, navigation, tab close) — unlike the previous "persistent until revoked" model, a user granting one-time access does NOT persist to the next visit. **Why this matters for GameZipper**: (a) games using `getUserMedia()` for AR features (camera-based games), voice-controlled games, or photo-capture games will lose their permission grant on every reload — the second launch silently fails with `NotAllowedError` instead of showing the camera; (b) games that assume persistent media permission (caching the granted state in localStorage) will display a broken camera/mic on second launch because the cached "permission granted" state is stale; (c) the one-time permission UI is different from the persistent permission UI — users may be confused by the re-prompt and deny, reducing engagement with camera/mic games; (d) the `navigator.permissions.query({name:'camera'})` API may return `granted` for the current session but the grant is gone after reload — code checking permissions before prompting will get false positives; (e) the one-click notification unsubscribe feature means game pages that requested notifications may have their notifications silently dropped if the user clicked unsubscribe. **QA methodology**: (1) test all `getUserMedia()` games: grant permission → use camera/mic → reload page → verify permission re-prompt appears (not silent failure); (2) test the one-time vs persistent permission UI — verify the user sees a clear choice; (3) check `navigator.permissions.query()` return value after reload with one-time grant — it should reflect the expired state; (4) test notification-dependent game features (e.g., "notify when opponent moves") after one-click unsubscribe; (5) audit game code for cached permission state that doesn't re-validate.

- [S-398] **[P2]** Firefox 150 Mythos AI — 180 High-Severity Bugs Confirmed Triggerable by Normal Browsing (so.html5.qq.com/page/real/search_news?docid=70000021_53769fd53ba77252 May 8 2026 — "Mozilla 团队借助 Anthropic 的 Mythos Preview AI 模型,在火狐 Firefox 浏览器 150 版本中,发现并修复了 271 个漏洞｡Mozilla 回应外界质疑,在最新博文中指出,在发现的 271 个漏洞中,有 180 个达到'高危'级别,意味着用户正常浏览网页就可能触发;此外 80 个为中危,11 个为低危"). **Why this is the AI-DISCOVERED-BROWSER-VULNERABILITY-AT-SCALE-WITH-CONFIRMED-SEVERITY dimension distinct from S-387 (Firefox Mythos initial — 271 bugs found) and S-389 (Google Big Sleep — 5 WebKit bugs)**: S-387 announced the discovery; S-389 is a different engine; S-398 captures the CONFIRMED-SEVERITY-BREAKDOWN class where Mozilla officially confirmed that 180 of the 271 AI-discovered Firefox bugs are HIGH severity and triggerable by normal browsing (not requiring special exploit payloads) — this means any ordinary web page, including GameZipper game pages, could be an unintentional trigger vector for these bugs on unpatched Firefox users. **Why this matters for GameZipper**: (a) 180 high-severity bugs triggerable by normal browsing means a GameZipper game page using standard web APIs (CSS, DOM, Canvas, WebGL, Web Audio) could unintentionally crash Firefox or trigger memory corruption on unpatched users; (b) Firefox users on versions < 150 are vulnerable — Firefox auto-updates but enterprise/locked-down environments may lag; (c) the scale (180 high-sev in one browser version) demonstrates that the "undetected bug backlog" in all browsers is enormous — GameZipper should assume ANY browser may crash on any game at any time, and implement graceful error recovery; (d) AI bug-hunting (Mythos, Big Sleep) is now production-grade — the rate of new CVE discoveries will accelerate, meaning today's safe code may be tomorrow's crash vector; (e) games that use unusual API combinations (e.g., WebGL + Web Audio + Canvas + Web Workers simultaneously) are more likely to hit undiscovered bugs. **QA methodology**: (1) test all games on Firefox 149 (pre-patch) and Firefox 150+ (patched) — compare for crashes, rendering differences; (2) implement global `window.onerror` + `unhandledrejection` handlers that log browser version + game slug for crash attribution; (3) for games using complex API combinations, add try/catch around initialization to provide graceful degradation; (4) monitor crash reports by browser version — a spike in Firefox < 150 crashes may indicate an AI-discovered bug affecting real users; (5) subscribe to Firefox security advisories and map newly disclosed bugs to GameZipper API usage.

### Performance (P)

- [P-152] **[P2]** Chrome 2026 WebGPU 2.0 + V8 Engine Overhaul — 30% Faster Load / 25% Lower Memory Changes Game Performance Baseline (ppzy.com/app/49612.html May 11 2026 — "Chrome2026版...全新V8引擎与 WebGPU2.0加持,页面加载提速30%,内存占用降低25%,百标签并发仍流畅稳定"). **Why this is the NEXT-GEN-GPU-API-AND-ENGINE-OVERHAUL dimension distinct from P-151 (Chrome Memory Saver tab discarding) and P-149 (iOS WebKit performance)**: P-151 is memory management; P-149 is mobile engine speed; P-152 captures the ENGINE-WIDE-OVERHAUL class where Chrome 2026 ships both WebGPU 2.0 (successor to WebGL) and a rewritten V8 JavaScript engine — the combination changes the performance characteristics of ALL games, not just GPU-bound ones. **Why this matters for GameZipper**: (a) WebGPU 2.0 may cause Chrome to de-prioritize WebGL (the current standard for 3D games) — WebGL games may see performance regression as GPU resources shift to WebGPU paths; (b) the new V8 JIT compiler changes optimization/deoptimization heuristics — game loops that were fast under old V8 may hit deoptimization cliffs under new V8 (e.g., polymorphic call sites, megamorphic inline caches); (c) timing-sensitive game logic (physics step, frame-rate-dependent movement) may behave differently — `performance.now()` resolution and `requestAnimationFrame` timing could shift; (d) memory allocation patterns change (25% lower memory claimed) — games that were near the memory limit may now fit, but games relying on specific GC timing for object pooling may see different collection cadence; (e) "100-tab smoothness" means Chrome may allocate less CPU per tab — game frame rates may be capped or throttled more aggressively in multi-tab scenarios. **QA methodology**: (1) benchmark all WebGL games on Chrome 2026 vs Chrome 2025 — compare FPS, frame timing variance (`requestAnimationFrame` delta); (2) test WebGPU feature detection — `navigator.gpu` presence — and verify WebGL fallback works; (3) profile V8 deoptimization via Chrome DevTools > Performance > JS profile — look for "deoptimized" entries in game loops; (4) test games with 10+ tabs open — verify frame rate stability; (5) test memory-intensive games (particle systems, large sprite sheets) — verify the 25% memory reduction doesn't cause over-allocation errors.

- [P-153] **[P2]** Chrome/Edge 2026 Battery-Saver Tab Freezing — Idle Game Tabs Frozen, Timers/RAF Halted Indefinitely (ppzy.com/app/49612.html May 11 2026 — "Chrome2026版...性能上,新增的省电模式通过冻结闲置标签有效降低能耗"). **Why this is the ENERGY-SAVER-TAB-FREEZE dimension distinct from P-151 (Memory Saver tab discarding — frees RAM) and P-150 (DevTools heap snapshot — leak detection)**: P-151 discards tabs to free RAM (page reloads on return); P-150 is a debugging tool; P-153 captures the CPU-ENERGY-FREEZE class where a dedicated battery-saver mode freezes background tabs' JavaScript execution (timers, rAF, event loops) WITHOUT discarding the page — the tab's state is preserved but its code stops running, and when the tab is unfrozen, all the missed time elapses at once. **Why this matters for GameZipper**: (a) a game using `setInterval`/`setTimeout` for timers (countdown, spawn intervals, cooldowns) will have those timers PAUSED during freeze — when unfrozen, they resume from where they left off, but `Date.now()` has advanced by the freeze duration, creating a massive time delta; (b) games using wall-clock time (`Date.now()` or `performance.now()`) for scoring, leaderboard timing, or cooldowns will compute incorrect values after unfreeze — a 10-minute freeze makes a 60-second level appear to take 10 minutes; (c) `requestAnimationFrame` is paused during freeze — game loops stop, canvas rendering halts, but the game state in memory is preserved; (d) real-time multiplayer games (WebSocket-based) will have their connection dropped or buffered during freeze — the server may time out the idle client; (e) unlike Memory Saver (which reloads the page), Battery Saver PRESERVES state but FREEZES execution — this is a new failure mode distinct from reload; (f) the freeze may last minutes to hours depending on battery level. **QA methodology**: (1) test each game: start playing → switch to another tab → enable battery saver → wait 5 minutes → return to game tab → verify game state is correct (not corrupted by time delta); (2) specifically test wall-clock-time games (timed puzzles, speedruns) — verify the timer doesn't jump after unfreeze; (3) test WebSocket games — verify reconnection after freeze; (4) test games using `setInterval` for spawns — verify no spawn-burst on unfreeze; (5) implement freeze-awareness: use `document.visibilitychange` + `performance.now()` delta detection to detect freeze and adjust game time accordingly.

### Summary — v1.174.0 New Test Cases

| ID | Cat | P | Title |
|----|-----|---|-------|
| W-329 | W | P2 | Edge 2026 Copilot UI Overhaul — Browser UI Convergence + AI Panel Auto-Open Changes Game Page Rendering |
| W-330 | W | P2 | HTML-in-Canvas API (WICG/Chromium) — Unified DOM+Canvas Rendering Changes Feature Detection & Screenshot Capture |
| S-396 | S | P2 | Safari 26 Fingerprinting Protection for Known Tracking Scripts — Blocklist Silently Fails Analytics/Ad Scripts |
| S-397 | S | P2 | Chrome 2026 One-Time Camera/Microphone Permissions — Single-Use Media Grant Expires After Session |
| S-398 | S | P2 | Firefox 150 Mythos AI — 180 High-Severity Bugs Confirmed Triggerable by Normal Browsing |
| P-152 | P | P2 | Chrome 2026 WebGPU 2.0 + V8 Engine Overhaul — 30% Faster Load / 25% Lower Memory Changes Game Performance Baseline |
| P-153 | P | P2 | Chrome/Edge 2026 Battery-Saver Tab Freezing — Idle Game Tabs Frozen, Timers/RAF Halted Indefinitely |

**Total new cases: 7. Version bumped v1.173.0 -> v1.174.0.**
