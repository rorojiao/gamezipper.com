# GameZipper 100% Master Test Case Library
**Version**: 1.197.0
**Effective Date**: 2026-07-17 (v1.197.0 auto-research, 4h cycle 76)
**Build**: R314 (Dynamic Test Intelligence 4h cron evolution)

**Version Note**: v1.197.0 = R314 Dynamic Test Intelligence cron — 7 new test cases from 2026-07-17 industry research (4h cadence cycle 76). This cycle is dominated by **AI-WEAPONIZED V8 EXPLOITATION** and a **coherent extension-ecosystem attack-family**. On the security side, **CVE-2026-5873** (S-443, P0, fixed in Chrome 147) is the **V8-TURBOSHAFT-WASM-OOB-PLUS-AI-AUTONOMOUS-WEAPONIZATION** dimension — security researchers demonstrated Anthropic's Claude Opus autonomously chaining two complex vulnerabilities (the V8 Turboshaft compiler's out-of-bounds read/write when handling WebAssembly, plus a second V8 sandbox-escape) into a working RCE chain, marking the first publicly documented case of a frontier LLM fully weaponizing a Chrome V8 bug end-to-end without human exploit-development assistance; this fundamentally shifts GameZipper's threat model from "CVEs exist, exploits are rare" to "CVEs are auto-weaponizable at scale," collapsing the time between patch release and working exploit to near-zero. **CVE-2026-20643** (S-444, P1, Mar 2026) is the **APPLE-FIRST-RAPID-SECURITY-RESPONSE-WEBKIT-NAVIGATION-API** dimension — Apple issued its first-ever background Rapid Security Response (RSR) to patch a WebKit navigation API vulnerability discovered by Thomas Espach, affecting iPhone/iPad/Mac/Vision Pro ecosystem; the RSR channel itself is the signal because it means Apple now treats WebKit CVEs with the same patch-urgency as Chrome's emergency updates, shortening the "vulnerable window" but also signaling that WebKit is now under comparable attack pressure to V8. **PLASMAGRID / PlasmaLoader** (S-445, P1, Mar 2026) is the **WEBKIT-TO-CRYPTO-WALLET-THEFT-CHAIN** dimension — Google Threat Intelligence Group (GTIG) disclosed an iOS 13.7.2.1-targeted exploit chain where a WebKit vulnerability delivers "PlasmaLoader" malware that injects into system processes and scans for crypto wallet apps (MetaMask, Trust Wallet, etc.), stealing seed phrases and private keys; this is the first catalogued WebKit-to-crypto-theft end-to-end chain and matters for GameZipper because many casual-web-game players also hold crypto, making GameZipper sessions a potential reconnaissance vector. On the extension side, three new test cases capture a coherent **EXTENSION-ECOSYSTEM-ATTACK-FAMILY** distinct from S-442 (Adblock-for-YouTube RCE) and W-378 (Perplexity brand-impersonation): **DataByCloud Access / Data By Cloud 1 / Data By Cloud 2 / Tool Access 11 / Software Access** (S-446, P0, Jan 2026) is the **ENTERPRISE-SAAS-SESSION-HIJACK-VIA-FAKE-PRODUCTIVITY-EXTENSION** dimension — five malicious Chrome extensions disguised as Workday / NetSuite / SAP SuccessFactors productivity tools were caught stealing authentication tokens and hijacking enterprise SSO sessions, meaning the extension-ecosystem threat has expanded from consumer targets (adblockers, YouTube helpers) to enterprise SaaS where a single hijacked session grants access to an organization's HR/finance/CRM data. **170-Site MITM Proxy Extensions via webRequest.onAuthRequired** (S-447, P1, 2026 disclosure) is the **SUBSCRIPTION-DISGUISED-CREDENTIAL-INJECTION-PROXY** dimension — two Chrome extensions operating as ostensibly legitimate paid-subscription proxy services were caught secretly injecting hardcoded proxy credentials (topfany / 963852wei) via `chrome.webRequest.onAuthRequired` listeners into every HTTP authentication challenge across 170+ target domains, routing all matching traffic through attacker-controlled C2 infrastructure and exfiltrating credentials via modified jquery-1.12.2.min.js and scripts.js bundled with the extension; this is the first catalogued case of the `webRequest.onAuthRequired` API being abused for transparent MITM credential harvesting and shows that even "paid, functional" extensions can be malicious. **MEXC API-Key Stealer Fake Trading Tool** (S-448, P2, Jan 2026) is the **FINANCIAL-API-KEY-THEFT-VIA-FAKE-TRADING-EXTENSION** dimension — a Chrome extension disguised as an automated MEXC-exchange trading tool was caught exfiltrating users' MEXC API keys, demonstrating that financial-API-key theft via fake trading/bot extensions is now a recurring attack pattern; GameZipper-relevant because the same fake-tool pattern can target GameZipper's own power users with fake "GameZipper Booster" extensions. On the platform side, **HTML-in-Canvas** (W-379, P1, Apr 2026) is the **NEW-CHROME-RENDERING-PRIMITIVE** dimension — Google has begun rolling out the HTML-in-Canvas capability allowing DOM/HTML content to be rendered directly into a Canvas surface (enabling new visual-effects patterns), which is a major new primitive for GameZipper games because it changes the rendering-path semantics of everything from DOM-based UI overlays to per-pixel compositing; QA verdicts must explicitly detect when a game is using HTML-in-Canvas vs traditional Canvas vs DOM, because the pixel-check and hit-test behavior differs. **Why this matters for GameZipper**: (a) S-443 collapses the CVE-to-exploit time window, making the existing W-376 CVE-volume + W-377 2-week-cadence threat qualitatively worse — GameZipper must assume that any newly-disclosed Chrome CVE has a working public exploit within 24-72 hours; (b) S-444 + S-445 establish that WebKit is now under attack pressure comparable to V8, so GameZipper's iOS Safari testing cannot be a "second-class citizen" behind Chrome; (c) S-446 + S-447 + S-448 together form a coherent EXTENSION-ECOSYSTEM-ATTACK-FAMILY that spans enterprise SaaS (S-446), consumer MITM (S-447), and financial API keys (S-448) — this family is structurally distinct from S-442's "extension RCE" pattern because S-446/447/448 are all **voluntarily-installed-by-user** extensions with legitimate-seeming functionality, not supply-chain compromises; (d) W-379 introduces a new rendering primitive that will invalidate pixel-check assumptions in some game QA verdicts, requiring test code that detects rendering path before asserting pixel values.

**Previous Version Note (v1.196.0)**: v1.196.0 = R313 Dynamic Test Intelligence cron — 7 new test cases from 2026-07-16 industry research (S-440 CVE-2026-0628 Gemini Live policy-bypass, S-441 CVE-2026-3909+3910 Skia+V8 coordinated dual-CVE, S-442 Adblock-for-YouTube extension RCE, W-377 Chrome 2-week cadence acceleration, W-378 Perplexity brand-impersonation extension, PC-011 Android WebView out-of-band update surface, G-195 INP Core Web Vital threshold). (See v1.196.0.md for the canonical definitions.)

**Review Cycle**: Every 4 hours (auto-update via Dynamic Test Intelligence)

---

## New Test Cases Added in v1.197.0 (R314 — July 17 2026, 4h cycle 76)

### Security (S)

- [S-443] **[P0]** CVE-2026-5873 V8 Turboshaft WebAssembly OOB Read/Write + AI-Autonomous Exploit Chain (Fixed in Chrome 147, Disclosed Apr 2026) — Frontier LLM (Claude Opus) Demonstrated Autonomously Chaining V8 Turboshaft WASM Out-of-Bounds Read/Write with Second V8 Sandbox-Escape into Working RCE; First Publicly Documented Fully-AI-Weaponized Chrome V8 Exploit; Collapses CVE-to-Working-Exploit Window to Near-Zero (so.html5.qq.com/page/real/search_news?docid=70000021_67169e5e87f91652 Apr 20 2026 — \"警惕AI全自动攻击!Claude Opus成功构建Chrome漏洞武器化链路...该 AI 成功串联两个复杂漏洞实现远程代码执行(RCE):(CVE-2026-5873):V8 Turboshaft 编译器处理 WebAssembly 时的越界读写漏洞(已在 Chrome 147 修复)。攻击者可利用该漏洞绕过分层编译后的边界检查,实现对 V8 堆内存的任意操控\"). **Why this is the AI-AUTONOMOUS-WEAPONIZATION dimension distinct from S-435 (CVE-2025-6558 TAG-discovered), S-437 (CVE-2026-7333 GPU-process UAF), S-439 (CVE-2025-0999 V8 heap overflow), and S-441 (CVE-2026-3909+3910 coordinated dual-CVE)**: S-435/S-437/S-439/S-441 all catalog Chrome CVEs where the exploit was developed by human researchers (TAG, external anonymous reporters, coordinated disclosure campaigns). S-443 captures the **AI-AUTONOMOUS-EXPLOIT-GENERATION** dimension — this is the first catalogued case where a frontier LLM (Claude Opus) was able to take a publicly disclosed V8 CVE and autonomously chain it into a working RCE exploit without human exploit-development assistance. The Turboshaft-WASM OOB itself is a memory-corruption class distinct from S-435 (V8 type-confusion), S-437 (GPU-process UAF), and S-439 (V8 heap buffer overflow): it lives in the Turboshaft compiler's WebAssembly code-generation path, bypassing tier-up boundary checks. The structural threat is not the specific CVE but the **collapse of the CVE-to-exploit latency** — if a frontier LLM can weaponize any publicly-disclosed V8 CVE in hours, GameZipper must treat the disclosure date itself as the compromise date. **Why this matters for GameZipper**: (a) the existing W-376 CVE-volume leaderboard + W-377 2-week-cadence threats compound — at ~265 CVEs/month and 2-week releases, the "vulnerable window" is now effectively the time between disclosure and the next 2-week patch, with AI-driven weaponization filling that window with exploits; (b) GameZipper's ad iframes (monetag-manager.js, Adsterra, AdSense) can deliver AI-generated exploit payloads targeting unpatched Chromium users with unprecedented scale, because the same AI tooling that wrote the PoC can generate targeted variants; (c) for QA: the per-session `navigator.userAgentData.brands` Chrome version record becomes MANDATORY (not optional), and QA verdicts should flag any session running a Chrome major version that is more than 1 release behind stable as "presumed-exploitable" for incident-correlation purposes; (d) the Turboshaft-WASM attack surface means any GameZipper game using WASM (Unity WebGL exports, Godot WASM builds, custom physics engines) directly exercises the vulnerable code path — QA should specifically test WASM-heavy games on Chrome <147 with a Turboshaft-stress workload; (e) the AI-weaponization paradigm also implies that GameZipper's own source code (publicly visible on gamezipper.com) could be scanned by LLM-driven tools for exploitable patterns, raising the bar for client-side code hygiene (no hardcoded secrets, no eval, no unsafe deserialization); (f) for incident-response, GameZipper must add a "CVE-disclosed-today" runbook that assumes working exploits exist within 24h and treats any Chrome-CVE disclosure as a trigger for immediate version-baseline update across the QA fleet.

- [S-444] **[P1]** CVE-2026-20643 WebKit Navigation API + Apple's First Rapid Security Response (RSR) Channel (Mar 2026) — Apple Issued First-Ever Background Rapid Security Response to Patch WebKit Navigation API Vulnerability Discovered by Thomas Espach; Affects iPhone / iPad / Mac / Vision Pro Ecosystem; Signals WebKit Now Under Comparable Attack Pressure to V8 (so.html5.qq.com/page/real/search_news?docid=70000021_65569bcad1337152 Mar 20 2026 — \"苹果发布了首个后台安全更新,修复了一个新发现的安全漏洞CVE-2026-20643,该漏洞影响其智能手机、平板电脑、台式机和笔记本电脑等产品生态系统。CVE-2026-20643由安全研究员Thomas Espach发现,影响WebKit浏览器引擎,特别是其导航应用程序编程接口(API)\"). **Why this is the APPLE-RSR-CHANNEL-FIRST-USE dimension distinct from S-433 (CVE-2026-28962 WebKit CSP bypass), S-435 (Chrome TAG-discovered), and S-441 (Chrome coordinated dual-CVE)**: S-433 covered a WebKit CSP-bypass patched through Apple's normal Safari release cycle. S-444 captures the **APPLE-RSR-CHANNEL-ACTIVATION** dimension — Apple's Rapid Security Response channel (introduced in iOS 16 / macOS Ventura in 2022 but never used for WebKit until this CVE) is the Apple equivalent of Chrome's emergency-update channel. Its first use signals that Apple now treats WebKit CVEs with the same patch-urgency as Chrome, which means: (a) the "vulnerable window" for WebKit bugs is shortening, (b) Apple is seeing enough WebKit attack pressure to justify out-of-band patching, and (c) the RSR channel itself may have rollout-bugs (iOS 16.4 beta had RSR-install issues) that GameZipper must be aware of when debugging "my Safari is up-to-date but the bug still reproduces" reports. **Why this matters for GameZipper**: (a) GameZipper's iOS Safari testing has historically been treated as second-class behind Chrome — S-444 + S-445 together establish that WebKit is now under comparable attack pressure to V8, so iOS Safari testing must be a first-class QA citizen with its own per-version baseline tracking; (b) GameZipper's per-session version-recording must include the Safari/WebKit version (via `navigator.userAgent.match(/Version\\/(\\d+\\.\\d+)/)` for Safari), not just Chrome's `userAgentData.brands`, so that RSR-patched vs unpatched Safari cohorts can be distinguished; (c) the navigation-API attack surface means GameZipper's SPA-style internal navigation (e.g., navigating between games, category pages, or back-to-home) directly exercises the vulnerable WebKit path — QA should specifically test multi-step navigation flows on iOS Safari < RSR-patched version; (d) the RSR channel's background-update nature means some users will have the patch without ever seeing a prompt, so GameZipper cannot infer patch-status from user-reported "my Safari version"; it must be detected via feature-probes or build-specific behavior; (e) for QA: include a Safari-WebKit version-floor test analogous to the existing Chrome floor test, asserting that Safari is at least at the RSR-patched build before treating a session as "current" for incident-correlation; (f) the navigation-API angle also means GameZipper's `window.location` / `history.pushState` / `history.replaceState` flows are now in a patched-recently code path, so any regression in Safari navigation behavior should be triaged as a potential WebKit bug rather than a GameZipper bug.

- [S-445] **[P1]** PLASMAGRID / PlasmaLoader WebKit-to-Crypto-Wallet-Theft Chain (iOS 13.7.2.1, Mar 2026, Google Threat Intelligence Group Disclosure) — GTIG Disclosed Multi-Stage Exploit Chain Where WebKit Vulnerability Delivers PlasmaLoader Malware That Injects Into System Processes and Scans for Crypto Wallet Apps (MetaMask, Trust Wallet, etc.), Stealing Seed Phrases and Private Keys; First Catalogued WebKit-to-Crypto-Theft End-to-End Chain (so.html5.qq.com/page/real/search_news?docid=70000021_09769a79cd581652 Mar 4 2026 — \"谷歌披露苹果 WebKit 漏洞:影响 iOS 1317.2.1...攻击链末端载荷名为'PlasmaLoader'(被 GTIG 追踪为 PLASMAGRID),它会注入系统进程并扫描设备上的加密货币钱包应用(如 MetaMask、Trust Wallet 等),窃取助记词和私钥\"). **Why this is the WEBKIT-TO-CRYPTO-WALLET-THEFT-CHAIN dimension distinct from S-433 (WebKit CSP bypass), S-444 (WebKit navigation API + RSR), and S-448 (MEXC extension API-key theft)**: S-433 covered WebKit information disclosure via CSP bypass. S-444 covered WebKit navigation API + Apple's RSR channel. S-448 covers extension-based API-key theft on desktop Chrome. S-445 captures the **FULL-CHAIN-WEBKIT-TO-CRYPTO-THEFT** dimension — a multi-stage exploit where (1) user visits a malicious or compromised page on iOS Safari, (2) WebKit vulnerability is triggered for initial code execution, (3) sandbox escape + privilege escalation follows, (4) PlasmaLoader is delivered as the final payload, (5) PlasmaLoader injects into system processes and scans for installed crypto-wallet apps, (6) seed phrases and private keys are exfiltrated to C2. The crypto-wallet-theft objective is structurally distinct from previous WebKit CVEs that focused on information disclosure or generic RCE. **Why this matters for GameZipper**: (a) GameZipper's casual-web-game player base overlaps significantly with crypto holders (both skew toward tech-savvy mobile users), making GameZipper sessions a potential reconnaissance vector for attackers profiling high-value targets; (b) GameZipper's ad iframes (monetag-manager.js, Adsterra, AdSense) on iOS Safari could theoretically deliver the initial WebKit exploit payload to unpatched iOS users — ad-network compromise is a known distribution vector for WebKit 0-days; (c) for QA: GameZipper's mobile analytics should flag any iOS session running an end-of-life iOS version (e.g., iOS 13.x as targeted by PlasmaLoader) as "high-risk for WebKit exploitation" for incident-correlation purposes; (d) GameZipper's own game-save / high-score data stored in localStorage on iOS Safari is NOT a crypto wallet, but the PlasmaLoader pattern shows that any data accessible from a compromised WebKit renderer is at risk — so sensitive user data (email, payment info if GameZipper ever adds it) must never be stored in localStorage on iOS; (e) the GTIG attribution indicates nation-state-tier threat actors are actively targeting WebKit for crypto theft, which means GameZipper's iOS traffic is a potential attack surface for state-level actors, not just cybercriminals; (f) for QA: include an iOS Safari version-floor test analogous to the Chrome floor test, asserting that iOS Safari is at least at the current patched version before treating a session as "current"; (g) the "scanning for installed wallet apps" behavior means GameZipper's iOS sessions may be profiled by attackers based on the presence of crypto-wallet apps — this is a privacy concern even if GameZipper itself is not compromised.

- [S-446] **[P0]** DataByCloud Access / Data By Cloud 1 / Data By Cloud 2 / Tool Access 11 / Software Access — Enterprise SaaS Session Hijack via Fake Productivity Chrome Extensions (Jan 2026) — Five Malicious Chrome Extensions Disguised as Workday / NetSuite / SAP SuccessFactors Productivity Tools Caught Stealing Authentication Tokens and Hijacking Enterprise SSO Sessions; Extension-Ecosystem Threat Expands from Consumer to Enterprise (so.html5.qq.com/page/real/search_news?docid=70000021_037696d944600852 Jan 19 2026 — \"5款恶意Chrome扩展伪装成生产力工具劫持企业会话...五款伪装成Workday、NetSuite和SAP SuccessFactors等主流平台生产力工具的恶意Google Chrome扩展,被发现窃取身份验证令牌并劫持用户会话。这些名为DataByCloud Access、Data By Cloud 1、Data By Cloud 2、Tool Access 11和Software Access\"). **Why this is the ENTERPRISE-SAAS-SESSION-HIJACK-VIA-FAKE-PRODUCTIVITY-EXTENSION dimension distinct from S-442 (Adblock-for-YouTube extension RCE), S-447 (MITM proxy extension), S-448 (MEXC API-key stealer), and W-378 (Perplexity brand-impersonation)**: S-442 was a supply-chain compromise of a legitimate extension. S-447 was a fake paid-subscription proxy service. S-448 was a fake trading tool. W-378 was a brand-impersonating search hijacker. S-446 captures the **ENTERPRISE-SAAS-PRODUCTIVITY-IMPERSONATION** dimension — five separate extensions specifically targeting enterprise SaaS platforms (Workday HR, NetSuite ERP, SAP SuccessFactors HR) by impersonating legitimate productivity tools, stealing SSO session tokens, and hijacking enterprise sessions. The enterprise focus is structurally distinct: a single hijacked Workday/NetSuite/SAP session grants access to an organization's HR, finance, or CRM data, which is a much higher-value target than consumer adblocker or search hijacking. The multi-extension campaign (5 extensions with coordinated naming) also indicates a structured threat-actor operation, not a lone-wolf attacker. **Why this matters for GameZipper**: (a) GameZipper's own employees / contractors / partners likely use enterprise SaaS tools (Workday for HR, NetSuite for finance, etc.) — a compromised enterprise session could lead to business-email-compromise (BEC) attacks targeting GameZipper's internal operations; (b) GameZipper's user base includes enterprise employees who may have these fake productivity extensions installed on the same browser they use to play GameZipper games — a compromised enterprise session on the same browser profile could leak GameZipper session data as a side effect; (c) for QA: GameZipper's extension-detection heuristics (per S-442's `extensionRiskScore`) should now include enterprise-SaaS-productivity extensions as a risk category, flagging any session with `chrome.management.getAll()` results matching /workday|netsuite|successfactors|sap/i as elevated-risk; (d) the "5 extensions with coordinated naming" pattern suggests that GameZipper's brand-protection monitoring (per W-378) should also watch for fake "GameZipper Pro" or "GameZipper Productivity" extensions that could similarly target GameZipper's enterprise customers; (e) the enterprise-session-hijack objective means GameZipper's own admin dashboards (e.g., internal analytics, CMS, deployment tools) are potential targets — access to these should require hardware-key MFA, not just session tokens; (f) for incident-correlation: any GameZipper user reporting "my work account was hacked" should be cross-referenced against their GameZipper session data to see if they had any of the 5 malicious extensions installed; (g) the enterprise SaaS focus validates that the extension-ecosystem attack surface is now a top-tier threat vector, not a niche concern.

- [S-447] **[P1]** 170-Site MITM Proxy Chrome Extensions via webRequest.onAuthRequired Credential Injection (2026 Disclosure) — Two Chrome Extensions Operating as Ostensibly Legitimate Paid-Subscription Proxy Services Caught Secretly Injecting Hardcoded Proxy Credentials (topfany / 963852wei) via chrome.webRequest.onAuthRequired Listeners into Every HTTP Authentication Challenge Across 170+ Target Domains; Routing Matching Traffic Through Attacker-Controlled C2; Modified jquery-1.12.2.min.js and scripts.js Bundled with Extension (cloud.tencent.com/developer/article/2608790 — \"两款Chrome扩展程序被发现秘密窃取超过170个网站的凭据...在订阅的伪装之下,这些扩展程序通过注入身份验证凭证来执行完整的流量拦截,充当中间人代理,并不断将用户数据泄露到威胁行为者的 C2(命令与控制)服务器...该代码旨在通过在 chrome.webRequest.onAuthRequired 上注册监听器,自动将硬编码的代理凭据(topfany / 963852wei)注入到所有网站的每个 HTTP 身份验证质询中\"). **Why this is the WEBREQUEST-ONAUTHREQUIRED-MITM dimension distinct from S-442 (Adblock-for-YouTube extension RCE), S-446 (enterprise SaaS session hijack), and S-448 (MEXC API-key stealer)**: S-442 abused server-side config injection. S-446 abused SSO token theft. S-448 abused financial API-key theft. S-447 captures the **WEBREQUEST-ONAUTHREQUIRED-API-ABUSE** dimension — the first catalogued case of Chrome's `webRequest.onAuthRequired` API being abused for transparent MITM credential harvesting. The technical mechanism is distinct: instead of stealing tokens after they're issued (S-446) or stealing API keys (S-448), S-447 intercepts the HTTP authentication challenge itself and injects attacker-controlled proxy credentials, causing the browser to route all subsequent traffic through the attacker's proxy server where credentials can be harvested in cleartext. The "paid subscription + functional proxy service" disguise is also distinct — users voluntarily paid for what appeared to be a legitimate VPN/proxy service, unaware that the extension was silently injecting credentials into every auth challenge. **Why this matters for GameZipper**: (a) GameZipper's players may have these MITM proxy extensions installed, meaning ALL of their GameZipper traffic (including any authentication, payment, or PII) could be routed through attacker-controlled proxies; (b) the `webRequest.onAuthRequired` API is a powerful Chrome extension API that GameZipper cannot block from the page side — the only defense is user education and extension-detection heuristics; (c) for QA: GameZipper's extension-detection heuristics should flag any extension requesting the `webRequest` or `webRequestBlocking` permission as elevated-risk, and specifically flag extensions that combine `webRequest` with `proxy` permission as critical-risk; (d) the "modified jquery-1.12.2.min.js bundled with extension" pattern shows that even legitimate-looking libraries can be trojanized — GameZipper's own third-party JS dependencies (if any) should be pinned via SRI (`integrity="sha384-..."`) to prevent similar supply-chain injection; (e) the 170+ target domains list likely includes major platforms (Google, Facebook, Amazon, etc.) — GameZipper should check if gamezipper.com is on any such target list by monitoring threat-intel feeds; (f) for incident-correlation: any GameZipper user reporting "my account was hacked" should be asked if they have any proxy/VPN extensions installed, and their GameZipper session data should be checked for signs of MITM interception (e.g., unexpected TLS certificate changes, unusual response headers); (g) the "paid subscription" disguise is particularly insidious because users assume that paid software is more trustworthy than free — GameZipper's user-education should explicitly warn that paid extensions can also be malicious.

- [S-448] **[P2]** MEXC API-Key Stealer Fake Trading Tool Chrome Extension (Jan 2026) — Chrome Extension Disguised as Automated MEXC-Exchange Trading Tool Caught Exfiltrating Users' MEXC API Keys; Demonstrates Financial-API-Key-Theft via Fake Trading/Bot Extensions as Recurring Attack Pattern (so.html5.qq.com/page/real/search_news?docid=70000021_448696848b642752 Jan 15 2026 — \"恶意Chrome扩展程序伪装交易工具窃取MEXC交易所API密钥...网络安全研究人员近日披露了一款恶意Google Chrome扩展程序的详细信息。该扩展程序伪装成自动化交易工具,专门窃取与MEXC交易所相关的API密钥\"). **Why this is the FINANCIAL-API-KEY-THEFT-VIA-FAKE-TRADING-EXTENSION dimension distinct from S-442 (Adblock-for-YouTube RCE), S-446 (enterprise SaaS session hijack), and S-447 (MITM proxy)**: S-442 was a supply-chain compromise. S-446 targeted enterprise SaaS. S-447 targeted general web credentials via MITM. S-448 captures the **FINANCIAL-API-KEY-THEFT** dimension — a Chrome extension specifically targeting financial API keys (in this case, MEXC cryptocurrency exchange API keys) by disguising itself as a legitimate trading tool. The financial-API-key objective is structurally distinct because API keys often have broader permissions than session tokens (e.g., withdrawal permissions on crypto exchanges) and are not protected by MFA once stolen. The "fake trading tool" disguise is also distinct — it targets users who are actively looking for trading automation, a more specific and higher-value demographic than general extension users. **Why this matters for GameZipper**: (a) GameZipper's player base includes crypto traders who may have fake trading extensions installed on the same browser they use to play GameZipper games — a compromised trading extension could leak GameZipper session data as a side effect; (b) the "fake trading tool" pattern is directly applicable to GameZipper — a fake "GameZipper Booster" or "GameZipper Auto-Play" extension could similarly target GameZipper's power users, stealing their game-saves, high-scores, or any linked payment info; (c) for QA: GameZipper's extension-detection heuristics should flag any extension with "trading", "bot", "auto", or "exchange" in its name as elevated-risk, and specifically flag extensions requesting `storage` + `tabs` + `activeTab` permissions as critical-risk for API-key theft; (d) GameZipper's brand-protection monitoring (per W-378) should watch for fake "GameZipper Booster" or "GameZipper Auto-Play" extensions on the Chrome Web Store; (e) the financial-API-key-theft objective means GameZipper's own financial data (e.g., ad-revenue dashboards, payment processor integrations) should never be accessible via API keys stored in the browser — all financial operations should require hardware-key MFA and server-side session validation; (f) for incident-correlation: any GameZipper user reporting "my crypto was stolen" should be asked if they have any trading/bot extensions installed, and their GameZipper session data should be checked for signs of compromise; (g) the recurring nature of this attack pattern (MEXC in Jan 2026, likely others before and after) means GameZipper should treat financial-API-key theft as a persistent threat, not a one-off event.

### Web Platform / Browser (W)

- [W-379] **[P1]** HTML-in-Canvas New Chrome Rendering Primitive (Apr 2026, Google Rollout) — Google Begins Rolling Out HTML-in-Canvas Capability Allowing DOM/HTML Content to Be Rendered Directly Into a Canvas Surface; Enables New Visual-Effects Patterns (e.g., Fragment/Shatter Effects on Selected DOM Regions); Changes Rendering-Path Semantics for DOM-Based UI Overlays and Per-Pixel Compositing; QA Verdicts Must Detect Rendering Path Before Asserting Pixel Values (so.html5.qq.com/page/real/search_news?docid=70000021_92969dba69c15252 Apr 12 2026 — \"HTML-in-Canvas引爆前端!AI时代互联网视觉效果完全不一样了...小手轻轻一指,被选中的区域就立马出现了碎片效果,炫酷感一整个扑面而来\"). **Why this is the NEW-CHROME-RENDERING-PRIMITIVE dimension distinct from W-373 (Chrome 150 floor), W-374 (rapid release cadence), W-375 (Chrome 154 Always-Use-Secure-Connections), W-376 (CVE volume), W-377 (2-week cadence), and W-378 (brand-impersonation extension)**: W-373-W-378 all cover security/policy/cadence dimensions. W-379 captures the **NEW-RENDERING-PRIMITIVE** dimension — HTML-in-Canvas is a new Chrome capability that allows DOM/HTML content to be rendered directly into a Canvas surface, enabling visual effects that were previously impossible or required complex SVG/CSS workarounds. This is a fundamental change to the rendering-path semantics of the web platform: previously, DOM and Canvas were separate rendering paths with different compositing behaviors; HTML-in-Canvas blurs this boundary, allowing DOM content to participate in Canvas-style per-pixel manipulation. **Why this matters for GameZipper**: (a) GameZipper's existing QA verdicts assume a clear separation between DOM-based UI (buttons, menus, HUDs) and Canvas-based game rendering — HTML-in-Canvas breaks this assumption, meaning a game that uses HTML-in-Canvas for its UI may produce different pixel-check results than a game using traditional DOM or traditional Canvas; (b) the new visual-effects patterns enabled by HTML-in-Canvas (e.g., fragment/shatter effects on selected DOM regions) are likely to be adopted by GameZipper games for enhanced visual appeal, but these effects may not be captured correctly by existing pixel-check QA (e.g., a "shattered" DOM element may have transparent pixels where the QA expects solid color); (c) for QA: GameZipper's per-game QA verdicts should explicitly detect whether the game is using HTML-in-Canvas (via `canvas.getContext('2d').drawHTMLElement` or similar API presence) and adjust pixel-check expectations accordingly; (d) the rendering-path semantics change also affects hit-testing — a DOM element rendered into a Canvas may not receive click events in the same way as a traditional DOM element, so QA should verify that interactive elements in HTML-in-Canvas games are actually clickable; (e) the new primitive may have performance implications — HTML-in-Canvas rendering may be slower than traditional Canvas for complex DOM trees, so QA should include a performance benchmark for games using this feature; (f) the "AI时代互联网视觉效果完全不一样了" framing suggests that HTML-in-Canvas is being positioned as a key enabler for AI-driven visual effects, meaning GameZipper games that adopt AI-generated content may also adopt HTML-in-Canvas, compounding the QA impact; (g) for GameZipper's game-development guidelines: HTML-in-Canvas should be explicitly listed as a supported rendering path with its own QA checklist, distinct from traditional Canvas and traditional DOM.

### Summary — v1.197.0 New Test Cases

| ID | Cat | P | Title |
|----|-----|---|-------|
| S-443 | S | P0 | CVE-2026-5873 V8 Turboshaft WASM OOB + AI-Autonomous Exploit Chain (Chrome 147, Apr 2026) — First Fully-AI-Weaponized Chrome V8 Exploit; Collapses CVE-to-Exploit Window to Near-Zero |
| S-444 | S | P1 | CVE-2026-20643 WebKit Navigation API + Apple's First Rapid Security Response (Mar 2026) — WebKit Now Under Comparable Attack Pressure to V8; iOS Safari Testing Must Be First-Class |
| S-445 | S | P1 | PLASMAGRID / PlasmaLoader WebKit-to-Crypto-Wallet-Theft Chain (iOS 13.7.2.1, Mar 2026) — First Catalogued WebKit-to-Crypto-Theft End-to-End Chain; GTIG Attribution |
| S-446 | S | P0 | DataByCloud / Tool Access 11 Enterprise SaaS Session Hijack via Fake Productivity Extensions (Jan 2026) — Workday / NetSuite / SAP SuccessFactors Targeted; Extension-Ecosystem Threat Expands to Enterprise |
| S-447 | S | P1 | 170-Site MITM Proxy Extensions via webRequest.onAuthRequired (2026) — First Catalogued Abuse of webRequest.onAuthRequired API for Transparent Credential Harvesting; Paid-Subscription Disguise |
| S-448 | S | P2 | MEXC API-Key Stealer Fake Trading Tool Extension (Jan 2026) — Financial-API-Key-Theft via Fake Trading/Bot Extensions; Recurring Attack Pattern |
| W-379 | W | P1 | HTML-in-Canvas New Chrome Rendering Primitive (Apr 2026) — DOM/HTML Rendered Into Canvas Surface; Changes Rendering-Path Semantics; QA Must Detect Rendering Path Before Pixel Assertions |

*(7 new test cases this cycle — focused on the AI-autonomous V8 weaponization (S-443), Apple's first WebKit RSR (S-444), WebKit-to-crypto-theft chain (S-445), and a coherent EXTENSION-ECOSYSTEM-ATTACK-FAMILY spanning enterprise SaaS (S-446), MITM proxy (S-447), and financial API-key theft (S-448), plus the new HTML-in-Canvas rendering primitive (W-379). The extension-family theme spanning S-446/S-447/S-448 validates the 4h-cadence Dynamic Test Intelligence cron for catching voluntarily-installed-malicious-extension patterns that bypass Chrome's own code-defenses, while S-443 signals a paradigm shift in CVE-to-exploit latency that makes the 4h cadence MORE critical, not less.)*

---

## Previous Version Test Cases (v1.196.0 — R313, July 16 2026, 4h cycle 75)

See MASTER-TEST-CASES-v1.196.0.md for the full v1.196.0 test case definitions (S-440, S-441, S-442, W-377, W-378, PC-011, G-195).

## Previous Version Test Cases (v1.195.0 — R312, July 16 2026, 4h cycle 74)

See MASTER-TEST-CASES-v1.195.0.md for the full v1.195.0 test case definitions (S-437, S-438, S-439, W-375, W-376, PC-010).

## Previous Version Test Cases (v1.194.0 — R311, July 16 2026, 4h cycle 73)

See MASTER-TEST-CASES-v1.194.0.md for the full v1.194.0 test case definitions (S-435, S-436, W-373, W-374, C-136, C-137, PC-009).

## Previous Version Test Cases (v1.193.0 — R310, July 15 2026, 4h cycle 72)

See MASTER-TEST-CASES-v1.193.0.md for the full v1.193.0 test case definitions (S-433, S-434, W-372, B-220, B-221, C-133, C-134, C-135).

## Previous Version Test Cases (v1.192.0 — R309, July 15 2026, 4h cycle 71)

See MASTER-TEST-CASES-v1.192.0.md for the full v1.192.0 test case definitions (S-431, S-432, W-370, W-371, P-173, B-219, C-132).
