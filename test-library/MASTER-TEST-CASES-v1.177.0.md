# GameZipper 100% Master Test Case Library
**Version**: 1.177.0
**Effective Date**: 2026-07-12 (v1.177.0 auto-research, 4h cycle 56)
**Build**: R294 (Dynamic Test Intelligence 4h cron evolution)

**Version Note**: v1.177.0 = R294 Dynamic Test Intelligence cron — 7 new test cases from 2026-07-12 industry research (4h cadence cycle 56): Safari Technology Preview 247 built-in MCP server for AI-agent-driven browser QA W-336 (so.html5.qq.com/page/real/search_news?docid=70000021_8316a45ac2425952 Jul 2 2026 — "苹果 WebKit 博客昨日(7月1日)发布博文,宣布在 Safari 技术预览版 247 中,引入支持 MCP 服务器,可用于加速开发和调试流程...编程智能体可检查网页,访问控制台日志和网络请求,抓取截图,并与页面元素交互...苹果列出的用途包括调试网站､识别 Safari 兼容性问题､性能分析､可访问性检查,以及验证页面和用户界面状态" — the BROWSER-NATIVE-MCP-QA-PROTOCOL dimension where Safari 247 ships a built-in MCP server that lets external AI agents inspect live Safari pages, read console logs, capture network requests, take screenshots, and interact with DOM elements — this fundamentally changes cross-browser QA methodology because Safari-specific bugs that were previously only detectable via manual Web Inspector or flaky WebDriverIO/Selenium remote sessions can now be detected by the same MCP-driven agent tooling already used for Chrome/Firefox; the risk dimension is that agents using Safari MCP may get DIFFERENT DOM/console snapshots than Chrome MCP due to WebKit's distinct rendering and error reporting, producing divergent QA verdicts); CVE-2025-14765 WebGPU Use-After-Free heap corruption S-402 (github.com/advisories/GHSA-GV8F-9G4R-FJ8Q Dec 17 2025 — "Use after free in WebGPU in Google Chrome prior to 143.0.7499.147 allowed a remote attacker to potentially exploit heap corruption via a crafted HTML page" + so.html5.qq.com/page/real/search_news?docid=70000021_429694359bf01052 Dec 18 2025 — "WebGPU释放后使用漏洞:CVE-2025-14765...这类内存破坏漏洞会导致程序在指针指向的内存被释放后仍继续使用该指针｡攻击者常利用此类错误执行任意代码或使应用崩溃" — the WEBGPU-UAF-REMOTE-EXPLOIT dimension where a crafted WebGPU page can corrupt the Chrome GPU process heap and potentially execute arbitrary code — high severity because any game page using WebGPU is an attack surface); CVE-2025-14766 V8 Out-of-Bounds Read/Write renderer sandbox escape S-403 (github.com/advisories/GHSA-GV8F-9G4R-FJ8Q + so.html5.qq.com/page/real/search_news?docid=70000021_429694359bf01052 Dec 18 2025 — "V8内存破坏漏洞:CVE-2025-14766...该漏洞被描述为'越界读写',允许攻击者读取或修改预期边界外的内存｡在浏览器环境中,通常可被用来突破渲染器沙箱或泄露敏感信息...此漏洞由安全研究员Shaheen Fazim于2025年12月8日报告" — the V8-OOB-SANDBOX-ESCAPE dimension where crafted JavaScript or WebAssembly can read/write outside allocated V8 heap bounds, potentially breaking the renderer sandbox and leaking cross-origin data — relevant to any game executing third-party/untrusted JS); ClickFix clipboard injection attack vector S-404 (ithome.com/microsoft Jul 3 2026 — "应对 ClickFix 威胁:Opera 推出首个浏览器原生剪切板攻击防御功能...这项名为 Paste Protect 的功能可实时监控剪贴板活动,检测用户复制或网站植入的潜在恶意命令,如果检测到威胁,复制操作会立即被阻止" — the CLIPBOARD-COMMAND-INJECTION dimension where attackers plant fake "copy this command" UI on game pages that, when pasted into a terminal/shell, executes malicious code — GameZipper games with "share" or "copy level code" features must ensure clipboard content is benign and cannot be mistaken for shell commands); Opera Paste Protect first browser-native clipboard defense B-200 (ithome.com/microsoft Jul 3 2026 — "Opera 推出首个浏览器原生剪切板攻击防御功能...Paste Protect...实时监控剪贴板活动" — the BROWSER-NATIVE-CLIPBOARD-DEFENSE-DIVERGENCE dimension where Opera gains a native clipboard-write interception that OTHER browsers lack, so games using clipboard.copy() / navigator.clipboard.writeText() may behave differently on Opera — some legitimate copy operations may be blocked or warn the user, breaking "copy share link" UX); Cloudflare AI Traffic Management fine-grained crawler control P-157 (chinaz.com Jul 3 2026 — "7月1日,Cloudflare推出AI流量管理工具,支持站长按爬虫类型精细化管控,打破了过去'一刀切'式屏蔽的局限,在保留搜索引擎曝光" — the AI-CRAWLER-GRANULAR-CONTROL dimension where Cloudflare's new tool lets site owners allow some AI bots (Google AI, search-indexing) while blocking others (training-data scrapers), affecting which AI systems can discover/index GameZipper game pages and how AI-generated traffic appears in analytics); Chrome 151 Skills feature user-stored AI prompts W-337 (so.html5.qq.com/page/real/search_news?docid=70000021_02569df582c01752 Apr 15 2026 — "2026年4月14日,Google给Chrome浏览器加了一个Skills的新功能——用户可以把常用的AI提示词存下来,下次在任何网页上一键复用" — the USER-AI-PROMPT-INJECTION-ON-PAGE dimension where Chrome 151 users can store and one-click apply custom AI prompts to ANY webpage, meaning a user could ask an AI to "summarize this game", "find the cheat", or "rewrite the rules" — the AI may inject DOM overlays or modify visible page content, which GameZipper's analytics and ad scripts may misinterpret as user interaction or may conflict with game UI).

**Review Cycle**: Every 4 hours (auto-update via Dynamic Test Intelligence)

---
---

## New Test Cases Added in v1.177.0 (R294 — July 12 2026, 4h cycle 56)

### Web Platform / Browser (W)

- [W-336] **[P2]** Safari Technology Preview 247 Built-in MCP Server for AI-Agent Browser QA — Cross-Browser QA Methodology Shift & Snapshot Divergence Risk (so.html5.qq.com/page/real/search_news?docid=70000021_8316a45ac2425952 Jul 2 2026 — "苹果 WebKit 博客昨日(7月1日)发布博文,宣布在 Safari 技术预览版 247 中,引入支持 MCP 服务器,可用于加速开发和调试流程...编程智能体可检查网页,访问控制台日志和网络请求,抓取截图,并与页面元素交互...苹果列出的用途包括调试网站､识别 Safari 兼容性问题､性能分析､可访问性检查,以及验证页面和用户界面状态"). **Why this is the BROWSER-NATIVE-MCP-QA-PROTOCOL dimension distinct from W-326 (Chrome 151 AI Mariner omnibox — discovery) and existing QA-tooling cases**: W-326 covers AI-suggested URLs changing discovery flow. W-336 captures the QA-METHODOLOGY-SHIFT class where Safari 247 ships a built-in MCP (Model Context Protocol) server, allowing external AI agents — Claude, Cursor, Windsurf, custom coding agents — to programmatically inspect a live Safari page, read its console logs, enumerate network requests, capture screenshots, and interact with DOM elements, all without Selenium/WebDriverIO/Playwright. **Why this matters for GameZipper**: (a) Safari-specific rendering bugs (CSS Grid Lanes from W-331, TouchList mutation from W-334, ITP cookie blocking from B-197) that were previously only detectable via manual Web Inspector or flaky remote-driver sessions can now be detected by the SAME MCP-driven agent tooling already used for Chrome/Firefox QA — the GameZipper QA skill's reliance on Kachilu/Camoufox for Safari testing can be augmented or replaced by Safari MCP; (b) the RISK dimension is that an agent using Safari MCP may receive DIFFERENT DOM snapshots, console output, and network traces than the same agent using Chrome DevTools MCP or Camoufox — WebKit reports some errors differently (e.g., CSS custom property fallbacks, `console.warn` vs `console.error` classification, `pointer-events` computed-value formatting), producing divergent QA verdicts where Chrome says PASS and Safari says FAIL for the same game; (c) the Safari MCP server must be explicitly enabled per-session, so QA agents must verify the MCP connection is active before trusting "Safari passed" results; (d) screenshots captured via Safari MCP use WebKit's rendering pipeline (ColorSync, CoreText) which may differ pixel-wise from Chrome screenshots, breaking pixel-diff regression baselines; (e) network-request enumeration via Safari MCP may not capture `beacon` / `sendBeacon` requests the same way Chrome DevTools Network panel does, causing analytics-verification false negatives; (f) the MCP server runs over a local socket — any agent connecting to it inherits the user's Safari session cookies, which is a security surface if the agent processes untrusted game URLs. **QA methodology**: (1) when this feature reaches Safari stable (expected Safari 27/28), integrate Safari MCP into the GameZipper QA skill as a first-class browser alongside Camoufox — replace the "Kachilu is flaky on Safari" workaround; (2) for any cross-browser QA verdict disagreement, manually reconcile by checking whether the difference is a real WebKit bug vs a snapshot-format divergence (console-level classification, screenshot ColorSync); (3) verify Safari MCP session isolation — each QA run should use a fresh Safari profile to avoid cookie/localStorage bleed between games; (4) establish pixel-diff baselines PER browser engine, not a single baseline, because WebKit/Chromium/Firefox screenshot pixels will legitimately differ; (5) audit the MCP connection for untrusted-URL handling — never let an agent connect Safari MCP to a game URL supplied by a third party without sandboxing. **Source**: so.html5.qq.com/page/real/search_news?docid=70000021_8316a45ac2425952 (Jul 2 2026).

- [W-337] **[P3]** Chrome 151 "Skills" Feature — User-Stored AI Prompts Applicable to Any Webpage May Inject/Modify Game Page Content (so.html5.qq.com/page/real/search_news?docid=70000021_02569df582c01752 Apr 15 2026 — "2026年4月14日,Google给Chrome浏览器加了一个Skills的新功能——用户可以把常用的AI提示词存下来,下次在任何网页上一键复用"). **Why this is the USER-AI-PROMPT-INJECTION-ON-PAGE dimension distinct from W-326 (Chrome 151 AI Mariner omnibox — URL suggestion) and B-199 (Edge Copilot sidebar — viewport overlay)**: W-326 is the omnibox suggesting URLs; B-199 is a sidebar reducing viewport. W-337 captures the IN-PAGE-AI-OVERLAY-MODIFICATION class where Chrome 151 users can save custom AI prompts ("Skills") and one-click apply them to ANY open webpage — the AI then reads the page DOM and may inject overlay UI, modify visible text, or surface summaries/answers on top of the page content. **Why this matters for GameZipper**: (a) a user (especially a power user or streamer) may apply a Skill like "summarize this page" or "find the objective" to a GameZipper game page — the AI's injected overlay may cover game UI elements, intercept clicks, or shift layout, producing a UX that GameZipper never designed and cannot control; (b) GameZipper's analytics scripts (event listeners on canvas, click tracking) may register the AI overlay's DOM mutations as user interactions, inflating engagement metrics; (c) ad scripts (Monetag/AdSense) may detect the AI-injected overlay as invalid inventory or as a policy violation (overlay on top of ads), potentially triggering ad-account penalties; (d) if a user applies a "cheat" or "auto-solve" Skill that reads game state via DOM scraping and injects hints, leaderboards / score-submission become vulnerable to AI-assisted cheating; (e) the Skills feature stores prompts in the user's Google account, so a user's saved "game cheats" Skill propagates across their devices — a single viral cheat Skill could degrade the integrity of score-based games site-wide. **QA methodology**: (1) test 3+ game pages in Chrome 151+ with a custom Skill applied (e.g., "describe what's on this page") — confirm game canvas/input still works and no critical UI is occluded; (2) verify analytics event listeners do not fire on AI-overlay DOM mutations — use MutationObserver in a test harness to detect unintended event triggers; (3) verify ad containers are not overlapped by AI overlays — check AdSense/Monetag policy compliance when an AI overlay is present; (4) for score-based games, evaluate server-side score validation to resist AI-assisted DOM-scraping cheats; (5) document that GameZipper cannot prevent users from applying Skills, but can detect overlay presence via `document.querySelector('[data-ai-skill-overlay]')`-style probes (Chrome may tag AI overlays with a data attribute) and warn competitive players. **Source**: so.html5.qq.com/page/real/search_news?docid=70000021_02569df582c01752 (Apr 15 2026).

### Security (S)

- [S-402] **[P1]** CVE-2025-14765 — WebGPU Use-After-Free Heap Corruption in Chrome < 143.0.7499.147 (github.com/advisories/GHSA-GV8F-9G4R-FJ8Q Dec 17 2025 — "Use after free in WebGPU in Google Chrome prior to 143.0.7499.147 allowed a remote attacker to potentially exploit heap corruption via a crafted HTML page" + so.html5.qq.com/page/real/search_news?docid=70000021_429694359bf01052 Dec 18 2025 — "WebGPU释放后使用漏洞:CVE-2025-14765...这类内存破坏漏洞会导致程序在指针指向的内存被释放后仍继续使用该指针｡攻击者常利用此类错误执行任意代码或使应用崩溃"). **Why this is the WEBGPU-UAF-REMOTE-EXPLOIT dimension distinct from S-388 (canvas fingerprinting) and S-389 (Google Big Sleep WebKit vulns)**: S-388 is fingerprinting; S-389 is AI-found WebKit bugs. S-402 captures the CRAFTED-WEBGPU-PAGE-REMOTE-CODE-EXECUTION class where a malicious or compromised game page using the WebGPU API can trigger a use-after-free in Chrome's GPU process, corrupting the heap and potentially achieving arbitrary code execution — the attacker only needs the victim to LOAD the page, no user interaction beyond navigation. **Why this matters for GameZipper**: (a) GameZipper currently uses Canvas2D / WebGL, NOT WebGPU, so the direct attack surface is low TODAY — but any future game that adopts WebGPU (for compute-shader particle systems, ML-based game AI, or high-performance 3D) immediately becomes a potential exploit vector if a vulnerability like CVE-2025-14765 is unpatched in the user's Chrome; (b) a compromised third-party script loaded on a GameZipper page (an ad creative from Monetag/AdSense, a tracking pixel) could itself use WebGPU to exploit an unpatched visitor — GameZipper would be the delivery vector even though its own code is benign; (c) users on older Chrome (< 143.0.7499.147) visiting GameZipper pages that happen to load WebGPU-using ads are at risk; (d) the bug is in Chrome's GPU PROCESS, which is sandboxed — but a GPU-process escape combined with a renderer bug (like CVE-2025-14766) forms a full chain; (e) this is the third major WebGPU UAF in Chrome's history (CVE-2021-37957, CVE-2022-2007, CVE-2025-14765), indicating WebGPU remains a recurring vulnerability hotspot — any GameZipper adoption of WebGPU must include a vendor-patch-monitoring process. **QA methodology**: (1) verify all GameZipper pages load in the LATEST stable Chrome (>= 143.0.7499.147) — if any user reports a Chrome crash on a GameZipper game page, treat it as a potential WebGPU exploit attempt by a third-party script, not just a "bug"; (2) audit all third-party scripts loaded on GameZipper pages (Monetag, AdSense, analytics, footer) for any WebGPU usage — `grep -rn 'navigator\\.gpu\\|requestAdapter' --include='*.js'` in vendored scripts; (3) if/when GameZipper adopts WebGPU, subscribe to Chrome security channel advisories and enforce a minimum-Chrome-version gate (`if (navigator.gpu && !isChromeVersionAtLeast(143, 0, 7499, 147)) { disableWebGPU(); fallbackToWebGL(); }`); (4) for ad scripts, request confirmation from Monetag/AdSense that their creatives do not invoke WebGPU; (5) monitor the GitHub Advisory Database (GHSA) feed for future WebGPU CVEs — the recurrence pattern (3 UAFs in 4 years) means more are expected. **Detection command**: `grep -rnE 'navigator\\.gpu|requestAdapter\\(|GPUCanvasContext' --include='*.js' --include='*.html' .` flags WebGPU usage. **Source**: github.com/advisories/GHSA-GV8F-9G4R-FJ8Q + so.html5.qq.com/page/real/search_news?docid=70000021_429694359bf01052.

- [S-403] **[P1]** CVE-2025-14766 — V8 Engine Out-of-Bounds Read/Write, Renderer Sandbox Escape via Crafted JavaScript/WebAssembly (github.com/advisories/GHSA-GV8F-9G4R-FJ8Q + so.html5.qq.com/page/real/search_news?docid=70000021_429694359bf01052 Dec 18 2025 — "V8内存破坏漏洞:CVE-2025-14766...该漏洞被描述为'越界读写',允许攻击者读取或修改预期边界外的内存｡在浏览器环境中,通常可被用来突破渲染器沙箱或泄露敏感信息...此漏洞由安全研究员Shaheen Fazim于2025年12月8日报告"). **Why this is the V8-OOB-SANDBOX-ESCAPE dimension distinct from S-402 (WebGPU UAF — GPU process) and S-386 (CVE-2026-20643 DarkSword WebKit RCE)**: S-402 is the GPU process; S-386 is a WebKit zero-click. S-403 captures the V8-RENDERER-OOB class where crafted JavaScript or WebAssembly triggers an out-of-bounds read/write in V8's JIT or turbofan optimizer, allowing the attacker to read/write memory outside allocated heap bounds — in a browser this can break the renderer sandbox (escaping the per-tab isolation) and leak cross-origin data or achieve code execution within the browser process. **Why this matters for GameZipper**: (a) every GameZipper game page executes JavaScript — any game is a potential trigger surface if the V8 version is vulnerable; (b) WebAssembly games (Unity/Emscripten-compiled, or future WASM-based games) are especially relevant because WASM compilation paths exercise V8's turbofan optimizer heavily, and OOB bugs often live in optimized WASM code paths; (c) a malicious ad creative or third-party script (Monetag/AdSense) executing on a GameZipper page could trigger CVE-2025-14766 to escape the renderer and read OTHER tabs' data (including banking tabs the user has open) — GameZipper would be the attack vector; (d) combined with CVE-2025-14765 (WebGPU UAF), the two form a full browser-compromise chain: V8 OOB → renderer escape → WebGPU UAF → GPU process escape → full system code execution; (e) users on unpatched Chrome (< the patch version) visiting ANY GameZipper page that runs third-party JS are at risk; (f) V8 OOB bugs are often JIT-specific — they may not reproduce in interpreted mode, making them hard to detect in normal QA but exploitable in production where JIT is always on. **QA methodology**: (1) ensure the GameZipper QA environment uses a Chrome version >= the patched version for CVE-2025-14766 — if QA reproduces a Chrome renderer crash on a game page, treat it as a potential V8 exploit, not a game bug; (2) for any new WASM-based game, run a fuzzing pass (Differential fuzzing: same WASM module in Chrome vs Firefox vs Safari — divergent crashes indicate a V8-specific bug); (3) audit third-party scripts for known-V8-bug-trigger patterns (extreme integer overflow, typed-array out-of-bounds patterns, turbofan deoptimization loops); (4) enforce Subresource Integrity (SRI) on all third-party script tags so a compromised CDN cannot inject V8-exploit-triggering code; (5) monitor V8 stable-channel release notes for OOB CVEs — the V8 team ships ~2-4 high-severity OOB CVEs per year, so this is a recurring class. **Source**: github.com/advisories/GHSA-GV8F-9G4R-FJ8Q + so.html5.qq.com/page/real/search_news?docid=70000021_429694359bf01052.

- [S-404] **[P2]** ClickFix Clipboard Command Injection Attack Vector — Malicious "Copy This" UI Planted on Game Pages Tricks Users into Pasting Shell Commands (ithome.com/microsoft Jul 3 2026 — "应对 ClickFix 威胁:Opera 推出首个浏览器原生剪切板攻击防御功能...这项名为 Paste Protect 的功能可实时监控剪贴板活动,检测用户复制或网站植入的潜在恶意命令,如果检测到威胁,复制操作会立即被阻止"). **Why this is the CLIPBOARD-COMMAND-INJECTION dimension distinct from S-387 (Firefox Mythos vulns) and S-400 (npm supply chain)**: S-387 is browser-memory vulns; S-400 is package-supply-chain. S-404 captures the SOCIAL-ENGINEERING-CLIPBOARD-INJECTION class where attackers display fake "copy this command" / "copy this fix" UI on a webpage (e.g., disguised as a game error message, a "fix your lag" prompt, or a fake CAPTCHA), and when the user pastes the copied text into a terminal/PowerShell/Run dialog, it executes a malicious command (malware download, credential theft). **Why this matters for GameZipper**: (a) if attackers compromise a third-party ad creative on a GameZipper page (Malvertising), they can render a fake "GameZipper Error — copy this command to fix" overlay that tricks players into pasting malware into their terminal; (b) GameZipper's own "copy share link" / "copy level code" features must ensure clipboard content is ALWAYS benign URLs/data, never anything that could be mistaken for a shell command (avoid leading `curl`, `wget`, `powershell`, `rm`, `npm`, `pip` tokens); (c) any user-generated content (custom level codes, shareable puzzle states) that gets copied to clipboard could be weaponized by a malicious user to craft a "level code" that is actually a shell command — if another user copies and pastes it into a terminal, it executes; (d) the ClickFix attack is especially effective on casual game audiences (younger users, less technical) who may follow on-screen "fix" instructions without suspicion; (e) Opera's new Paste Protect (see B-200) blocks this on Opera, but Chrome/Safari/Firefox users remain unprotected. **QA methodology**: (1) audit ALL GameZipper clipboard-write calls (`navigator.clipboard.writeText`, `document.execCommand('copy')`) — confirm copied content matches an allowlist (URL, JSON, level-code format) and contains NO shell-interpretable tokens; (2) for any user-generated content that becomes clipboard text, sanitize against a denylist regex (`^(curl|wget|powershell|cmd|rm|bash|sh|npm|pip|python)\\b`) and reject/truncate matches; (3) test that fake "copy this to fix" overlays from compromised ads are detected — monitor ad iframes for clipboard-write attempts and block any ad creative that calls `navigator.clipboard.writeText` with non-URL content; (4) educate via a one-time toast on game pages: "GameZipper will NEVER ask you to copy a command to a terminal — any such prompt is fake"; (5) verify the post-deploy-verification Phase 0.5 dead-endpoint detector also scans for ad scripts that inject fake error UI. **Detection command**: `grep -rnE "clipboard\\.writeText|execCommand\\(['\"]copy" --include='*.js' --include='*.html' .` flags all clipboard-write sites for audit. **Source**: ithome.com/microsoft (Jul 3 2026, Opera Paste Protect report on ClickFix threat).

### Browser/Compatibility (B)

- [B-200] **[P3]** Opera Paste Protect — First Browser-Native Clipboard Attack Defense Creates Cross-Browser "Copy" Behavior Divergence (ithome.com/microsoft Jul 3 2026 — "应对 ClickFix 威胁:Opera 推出首个浏览器原生剪切板攻击防御功能...这项名为 Paste Protect 的功能可实时监控剪贴板活动,检测用户复制或网站植入的潜在恶意命令,如果检测到威胁,复制操作会立即被阻止"). **Why this is the BROWSER-NATIVE-CLIPBOARD-DEFENSE-DIVERGENCE dimension distinct from S-404 (ClickFix — the attack itself) and B-198 (Firefox Nova redesign — layout)**: S-404 covers the attack vector; B-198 is viewport layout. B-200 captures the CROSS-BROWSER-CLIPBOARD-BEHAVIOR-DIVERGENCE class where Opera gains a native Paste Protect that INTERCEPTS clipboard writes in real-time and BLOCKS the copy if the content looks like a malicious shell command — but Chrome/Safari/Firefox/Edge have NO equivalent native defense. **Why this matters for GameZipper**: (a) a legitimate GameZipper feature that copies a share link or level code MAY be falsely classified as a "malicious command" by Opera's Paste Protect heuristic — the user clicks "Copy Share Link" but nothing lands in their clipboard, and Opera may show a warning, confusing the user; (b) the divergence means a "copy" that works on Chrome/Firefox silently fails on Opera — QA that doesn't test Opera will miss this; (c) Opera's heuristic is opaque (proprietary pattern matching), so GameZipper cannot predict which level codes or share URLs will be blocked — long base64 level codes that happen to start with `curl`-like or `powershell`-like byte sequences are at risk; (d) the defense blocks the WRITE, not the READ — so games that READ clipboard (paste-handler games like Wordle-style games accepting pasted words) are unaffected, but games that WRITE clipboard (share features) are; (e) as other browsers (Chrome, Edge) are likely to follow Opera with similar native clipboard defenses (the ClickFix threat is industry-wide), this divergence will widen before it converges — GameZipper must test clipboard features across ALL browsers, not just Chrome. **QA methodology**: (1) test all GameZipper "copy" features (share link, copy level code, copy score) in Opera with Paste Protect enabled — confirm the copy succeeds or, if blocked, the UI shows a clear fallback ("copy failed — here's the text to select manually"); (2) for any level-code format that could resemble a shell command, add a non-command prefix (e.g., `GZ-` prefix) so Opera's heuristic doesn't flag it; (3) monitor Opera's Paste Protect documentation for the heuristic rules and adjust level-code format to avoid false positives; (4) provide a manual-select fallback UI next to every copy button (`<textarea readonly>` with the text pre-selected) so users on blocking browsers can still copy via Ctrl+C; (5) when Chrome/Edge ship equivalent defenses, re-test all copy features in those browsers too. **Detection command**: `grep -rnE "navigator\\.clipboard\\.writeText|execCommand\\(['\"]copy['\"]\\)" --include='*.js' --include='*.html' .` then manually test each hit in Opera Beta with Paste Protect ON. **Source**: ithome.com/microsoft (Jul 3 2026).

### Performance (P)

- [P-157] **[P2]** Cloudflare AI Traffic Management — Granular Per-Crawler Control Affects Game Analytics, Ad Attribution & AI Discovery (chinaz.com Jul 3 2026 — "7月1日,Cloudflare推出AI流量管理工具,支持站长按爬虫类型精细化管控,打破了过去'一刀切'式屏蔽的局限,在保留搜索引擎曝光"). **Why this is the AI-CRAWLER-GRANULAR-CONTROL dimension distinct from S-388 (fingerprinting) and P-149 (iOS WebKit perf)**: S-388 is client-side fingerprinting; P-149 is engine performance. P-157 captures the EDGE-AI-CRAWLER-POLICY class where Cloudflare's new AI Traffic Management tool lets site owners (including GameZipper, which sits behind Cloudflare) allow or block AI crawlers PER-CRAWLER rather than all-or-nothing — e.g., allow Google-Extended (Google AI search indexing) but block GPTBot (OpenAI training), or allow ClaudeBot (Anthropic) but block Bytespider (ByteDance). **Why this matters for GameZipper**: (a) GameZipper sits behind Cloudflare (per the CDN cache-busting pitfall in post-deploy-verification) — the AI Traffic Management setting directly controls which AI systems can discover, index, and summarize GameZipper game pages, affecting AI-search-driven traffic (Google AI Overviews, Perplexity, ChatGPT search); (b) overly permissive settings let training-data crawlers (GPTBot, Bytespider, CommonCrawl-Bot) scrape all game content for free, potentially enabling competitors to clone GameZipper's game catalog via AI training; (c) overly restrictive settings (block all AI bots) remove GameZipper from AI-search results, losing the growing ~5-15% of traffic that arrives via AI assistants instead of traditional Google search; (d) AI-crawler traffic appears in Google Analytics / Monetag analytics as bot traffic — without granular filtering, it inflates "users" metrics and may trigger ad-fraud false positives (Monetag/AdSense may flag AI-bot page-views as invalid traffic, reducing ad revenue); (e) the per-crawler policy must be coordinated with `robots.txt` — a mismatch (Cloudflare allows but robots.txt disallows, or vice versa) creates inconsistent behavior where some crawlers respect robots.txt and others get blocked at the edge; (f) some AI crawlers rotate user-agents or spoof Googlebot to evade blocking — Cloudflare's tool uses fingerprinting beyond user-agent, but no detection is perfect. **QA methodology**: (1) audit current Cloudflare AI Traffic Management settings for gamezipper.com — document which AI crawlers are allowed vs blocked and the business rationale; (2) verify `robots.txt` is consistent with the Cloudflare policy (no contradictions); (3) in Google Analytics / Monetag, create a bot-traffic segment filtered by known AI-crawler user-agents (GPTBot, ClaudeBot, Bytespider, PerplexityBot, Google-Extended) — monitor this segment's size weekly to detect surges; (4) test that blocking an AI crawler in Cloudflare actually blocks it (curl with the matching user-agent should get 403); (5) verify ad-fraud detection (Monetag/AdSense) is not falsely flagging AI-bot traffic — compare revenue before/after a policy change; (6) re-evaluate the policy quarterly as new AI crawlers emerge and as AI-search traffic share grows. **Detection command**: `curl -sI -A "GPTBot/1.0" https://gamezipper.com/ | head -1` should return 403 if GPTBot is blocked, 200 if allowed. **Source**: chinaz.com (Jul 3 2026, Cloudflare AI Traffic Management launch).

### Summary — v1.177.0 New Test Cases

| ID | Cat | P | Title |
|----|-----|---|-------|
| W-336 | W | P2 | Safari Technology Preview 247 Built-in MCP Server for AI-Agent Browser QA — Cross-Browser QA Methodology Shift & Snapshot Divergence Risk |
| W-337 | W | P3 | Chrome 151 "Skills" Feature — User-Stored AI Prompts Applicable to Any Webpage May Inject/Modify Game Page Content |
| S-402 | S | P1 | CVE-2025-14765 — WebGPU Use-After-Free Heap Corruption in Chrome < 143.0.7499.147 |
| S-403 | S | P1 | CVE-2025-14766 — V8 Engine Out-of-Bounds Read/Write, Renderer Sandbox Escape via Crafted JavaScript/WebAssembly |
| S-404 | S | P2 | ClickFix Clipboard Command Injection Attack Vector — Malicious "Copy This" UI Planted on Game Pages Tricks Users into Pasting Shell Commands |
| B-200 | B | P3 | Opera Paste Protect — First Browser-Native Clipboard Attack Defense Creates Cross-Browser "Copy" Behavior Divergence |
| P-157 | P | P2 | Cloudflare AI Traffic Management — Granular Per-Crawler Control Affects Game Analytics, Ad Attribution & AI Discovery |
