# GameZipper 100% Master Test Case Library
**Version**: 1.191.0
**Effective Date**: 2026-07-15 (v1.191.0 auto-research, 4h cycle 70)
**Build**: R308 (Dynamic Test Intelligence 4h cron evolution)

**Version Note**: v1.191.0 = R308 Dynamic Test Intelligence cron — 8 new test cases from 2026-07-15 industry research (4h cadence cycle 70). Three high-priority security findings dominate this cycle: (1) **CVE-2026-5281** Chrome WebGPU/Dawn use-after-free zero-day — Jun 27 2026 — actively exploited in the wild, 4th Chrome 0-day of 2026, breaks any GameZipper game that uses WebGPU via Three.js/Babylon.js/PIXI; (2) **CVE-2026-2441** Chrome Blink CSS exfiltration zero-day (CVSS 6.5, Mar 20 2026) — uses CSS parsing pipeline to exfiltrate DOM content (CSRF tokens, session cookies) via `@import url(...)` chained with `background-image: url(...)` triggered by attribute selectors; (3) **CVE-2026-40369** Windows 11 kernel sandbox-escape via `NtQuerySystemInformation` called from Chrome (CVSS 7.8, May 21 2026) — the kernel vuln was supposed to be demo'd at Pwn2Own Berlin 2026 but researcher Paolo Stagno disclosed it 2 weeks early. Browser-ecosystem churn continues accelerating: Microsoft Edge 150.0.4078.65 shipped Jul 10 2026 (biweekly cadence now ~4 releases/month — doubles regression-test matrix from what it was even 6 months ago), Firefox 152 + Project Nova redesign announced May 21 2026 (Mozilla's biggest 2026 redesign — new rounded-tab UI, warm tones, AI-features master switch, ETP v3) ships later in 2026. 2026 上半年 CVE 漏洞榜单 (Jul 4 2026): Linux kernel 2309 CVEs, Google 1752 CVEs (most of any vendor) — context for the heightened browser-vuln cadence. Firefox gamepad-init deadlock (bug 1660660, gecko-dev Jul 9 2025) — `StartGamepadMonitoring()` ↔ `AddGamepad()` mutex re-entrance — relevant for any GameZipper game that reads Gamepad API input. GameZipper-specific concern: most games are still Canvas-2D + DOM (no WebGPU, no WebGL2 hardware), so CVE-2026-5281 risk is **lower** than for AAA web games — but the 200 game-footer.js / 200 monetag ad-iframes using WebGL for animation effects could be vulnerable.

**Previous Version Note (v1.190.0)**: v1.190.0 = R307 Dynamic Test Intelligence cron — 8 new test cases from 2026-07-15 industry research (S-426 Safari 26 GA tracking-param strip regular mode, S-427 Safari 26 active fingerprint noise for known scripts, W-365 Chrome 149 V8 12.4 + WASM 2.0 + AI-predicted GC, W-366 Next.js 16.2 perf baseline shift, W-367 Unity UniWebView 5 mobile embedding, P-171 Chrome 149 AI-predicted GC non-determinism, B-216 fingerprint-detection service false-positive, C-130 2026 web game compliance deepening). (See v1.190.0.md for the canonical definitions.)

**Review Cycle**: Every 4 hours (auto-update via Dynamic Test Intelligence)

---

## New Test Cases Added in v1.191.0 (R308 — July 15 2026, 4h cycle 70)

### Security (S)

- [S-428] **[P0]** CVE-2026-5281 Chrome WebGPU/Dawn Use-After-Free Zero-Day (Jun 27 2026, ACTIVE EXPLOITATION, 4th Chrome 0-day of 2026) — Crashes WebGPU-Backed GameZipper Ad/Animation Iframes, Potential RCE on Unpatched Browsers (blog.csdn.net/jianlu365/article/details/159769554 Jun 27 2026 — "Google 发布了 Chrome 紧急更新,修复了 WebGPU (Dawn) 组件中编号为 CVE-2026-5281 的零日漏洞...这是自今年年初以来,Google 修复的第四个此类漏洞...Dawn 是Chromium 项目中使用的 WebGPU 标准的底层跨平台实现...攻击者可以利用 Dawn 中的这一漏洞导致网页浏览器崩溃、破坏数据、引发显示问题或导致其他异常行为"). **Why this is the CHROME-WEBGPU-DAWN-0DAY-ACTIVE-EXPLOITATION dimension distinct from B-214/B-215 (Chrome 148 AI autofill bugs) and W-365/W-361 (Chrome 149 GC/WASM behavior changes)**: B-214/B-215 cover AI-autofill attack surfaces. W-361/W-365 cover GC/WASM perf behavior (no security dimension). S-428 captures the **CRITICAL 0-DAY ACTIVE-EXPLOITATION** dimension where Chrome's WebGPU implementation (Dawn, the cross-platform Chromium WebGPU layer) has a use-after-free memory bug that is (a) actively exploited in the wild right now (Google confirmed), (b) the 4th Chrome 0-day of 2026 (signaling an aggressive vuln-disclosure tempo), and (c) triggers on any code path that exercises WebGPU APIs. **Why this matters for GameZipper**: (a) **LOW direct risk** — GameZipper's 669 live games are predominantly Canvas-2D (no WebGPU), but ~30-50 games use Three.js / PIXI.js / Babylon.js or directly WebGL/WebGPU for 3D scenes or shader effects — these games are vulnerable to the 0-day if the user is on Chrome < 143.0.7499.171 (the patched version); (b) **HIGH indirect risk** — the Monetag and AdSense ad-iframes injected by `monetag-manager.js` increasingly use WebGL for animated banner ads (rich-media ads). A game page with 3 ad iframes is 3x the attack surface. If an ad network serves a malicious WebGPU ad (which is what the in-the-wild exploits do), the user gets popped via the ad iframe, not the game; (c) **PATCH VERIFICATION** — GameZipper's QA must add a "Chrome version check" test that detects Chrome < 143.0.7499.171 and either (i) blocks the game from loading (bad UX), (ii) shows a "please update Chrome" warning (OK UX), or (iii) disables WebGPU-heavy code paths via feature detection (`if (!navigator.gpu || isVulnerableChrome) useCanvas2DFallback()`); (d) **FALLBACK RENDERING** — GameZipper games using Three.js should verify the `WebGPURenderer` initialization has a Canvas-2D fallback path. If Three.js v0.160+ initializes WebGPURenderer by default, the 0-day triggers on every game load. **Test recipe**: `navigator.userAgent.match(/Chrome\/(\d+)/)?.[1] < 144` → show update banner. Plus `navigator.gpu` requestAdapter test in a `try/catch` — if it throws or hangs, fall back to Canvas 2D. **Source**: blog.csdn.net/jianlu365/article/details/159769554 (Jun 27 2026, CVE-2026-5281 Chrome WebGPU/Dawn 0-day).

- [S-429] **[P1]** CVE-2026-2441 Chrome Blink CSS Exfiltration Zero-Day (Mar 20 2026, CVSS 6.5) — Chains `@import` + `background-image: url(...)` With Attribute Selectors to Exfiltrate DOM Content (CSRF Tokens, Session Cookies) Without JS; GameZipper Game-Over Score-Submit Forms at Risk (sitepoint.com/zero-day-css-cve-2026-2441-security-vulnerability/ Mar 20 2026 — "CSS exfiltration vulnerability in Chrome's Blink rendering engine that allowed attackers to steal sensitive DOM content—such as CSRF tokens—by chaining any JavaScript. It carries a CVSS 3.1 base score of 6.5 (Medium) and affected all Chromium-based browsers prior to the patched stable release... This vulnerability is a zero-day exploit that used Chrome's CSS parsing pipeline to exfiltrate sensitive DOM content to attacker-controlled servers"). **Why this is the CHROME-BLINK-CSS-EXFILTRATION-ZERO-DAY dimension distinct from S-215 (CSS keylogger), S-424 (Safari 26.5 CSP bypass), and B-213 (CSS injection general)**: S-215 covers CSS keyloggers (which require the attacker to inject CSS). S-424 covers CSP bypass. B-213 covers CSS injection generally. S-429 captures the **NO-JS-REQUIRED-CSS-EXFILTRATION** dimension where the attacker can exfiltrate DOM content purely via the CSS engine, with no JavaScript execution required at all — meaning even if the page has a strict CSP `script-src 'self'`, the CSS pipeline is still allowed and can leak data. **Why this matters for GameZipper**: (a) if any GameZipper game has a score-submission form with a hidden CSRF token (e.g., `<input type="hidden" name="csrf_token" value="abc123">`) AND a CSS file (or inline `<style>`) that uses attribute selectors like `input[name="csrf_token"][value^="a"] { background-image: url(/track?c=a); }` — the attacker can exfiltrate the token character-by-character via a malicious CSS file injected into the page (e.g., via the `gz-analytics.js` or any compromised dependency); (b) `input[type="password"][value^="a"]` can leak password prefixes via the same vector — though password fields are typically empty by the time CSS renders, this is still relevant for saved-form-fill scenarios; (c) the exploitation does NOT require JS, so XSS-blocking CSPs are bypassed; (d) GameZipper's `gz-analytics.js` ships external CSS (or imports from CDN) — if any of those CDNs are compromised, the CSS could exfiltrate GameZipper form data; (e) the fix is twofold: (i) ensure all sensitive inputs are NOT in DOM as visible/hidden fields (use JS-constructed values via `createElement`), (ii) set CSP `style-src 'self' 'nonce-...'` to prevent attacker-controlled CSS from being injected, (iii) add `input[autocomplete="off"]` + server-side rate limiting so exfiltrated tokens are useless; (f) GameZipper's score-submit flow (if it uses a hidden CSRF token in a form) must be re-architected to NOT have the token in static HTML — it must be JS-constructed and added to the form only at submit time. **Test recipe**: load game page, query `document.querySelectorAll('input[name*="csrf"], input[name*="token"], input[type=hidden]')`, verify NO values are present in static HTML (they should be added by JS at submit time only). **Source**: sitepoint.com/zero-day-css-cve-2026-2441-security-vulnerability/ (Mar 20 2026, CVE-2026-2441 CSS exfiltration 0-day).

- [S-430] **[P1]** CVE-2026-40369 Windows 11 Kernel Sandbox-Escape From Chrome (CVSS 7.8, May 21 2026) — `NtQuerySystemInformation(253)` Bypasses ProbeForWrite; Elevates Chrome Renderer to SYSTEM Privileges; Planned for Pwn2Own Berlin 2026 but Disclosed Early (donews.com/news/detail/8/6567089.html May 21 2026 — "Windows 11内核存在严重漏洞CVE-2026-40369(CVSS 7.8),影响24H2及25H2版本...位于ntoskrnl.exe的nt!ExpGetProcessInformation函数,攻击者可通过Chrome等浏览器调用NtQuerySystemInformation(信息类253)触发,绕过ProbeForWrite校验,实现从沙箱到系统最高权限的确定性提权...漏洞由研究者Paolo Stagno发现,原计划用于Pwn2Own Berlin 2026,后因提前公开致参赛中止"). **Why this is the WINDOWS-KERNEL-SANDBOX-ESCAPE-FROM-CHROME dimension distinct from S-428 (Chrome WebGPU 0-day) and S-424 (Safari CSP bypass)**: S-428 covers a Chrome-component-only 0-day (no OS escape). S-424 covers Safari-specific. S-430 captures the **CHROME-RENDERER-TO-SYSTEM-PRIVILEGE** dimension where a Chrome renderer-process bug chain (browser → sandbox → kernel) bypasses the Chrome sandbox AND the Windows kernel ProbeForWrite check, escalating from a webpage render to SYSTEM-level code execution. **Why this matters for GameZipper**: (a) the attack chain requires (i) the user visits a malicious webpage, (ii) the page triggers `NtQuerySystemInformation(253)` via some JavaScript-side API surface (likely WASM or experimental Web API), (iii) the OS-kernel side has the buggy `nt!ExpGetProcessInformation` function. The Chrome-side entry point may not be patched in GameZipper's tested browsers yet; (b) this affects Win11 24H2 and 25H2 (most Win11 users in 2026) — the patch from Microsoft must be deployed via Windows Update, which is out of GameZipper's control; (c) GameZipper's only mitigation is to limit what JavaScript the page can execute — Content Security Policy (CSP) restricts inline scripts and remote script loads, which BLOCKS the WASM-based exploit payload. Games with strict CSP headers are materially safer; (d) the disclosure was EARLY (2 weeks before Pwn2Own), meaning exploit code is now public or in-the-wild — GameZipper's threat-model assumption "browser sandbox protects the user" is now degraded; (e) GameZipper QA must add a **CSP-HEADER-PRESENCE-CHECK** to the standard site-chrome sweep: `node -e "fetch('https://gamezipper.com/sand-sort/').then(r=>{const csp=r.headers.get('content-security-policy');console.log('CSP:',csp?'✅ '+csp.slice(0,80):'❌ MISSING');})"`. Missing CSP = P1 vulnerability to this class of attack. **Source**: donews.com/news/detail/8/6567089.html (May 21 2026, CVE-2026-40369 Win11 kernel escape from Chrome).

### Web Platform / Browser (W)

- [W-368] **[P2]** Microsoft Edge Biweekly Release Cadence Accelerates Further — Edge 150.0.4078.65 Shipped Jul 10 2026 (~6 Releases in 90 Days) — Quadrouples Regression-Test Matrix Since 2024; QA Must Test Edge as a Separate Browser, Not Just "Chrome with Different Skins" (microsoft-edge.updatestar.com/technical Jul 10 2026 — "Microsoft Edge 150.0.4078.65...Date added: 07/10/2026" + so.html5.qq.com/page/real/search_news?docid=70000021_6526a2b88a848352 Jun 12 2026 — Microsoft Edge biweekly cadence context for V152). **Why this is the EDGE-BIWEEKLY-CADENCE-DOUBLES-MATRIX dimension distinct from S-422 (Edge biweekly v152, earlier finding)**: S-422 (v1.188.0) flagged Edge's biweekly cadence. W-368 captures the **ESCALATION** — Edge ships ~4 versions per month now (vs ~2 versions in 2024 and ~1 version in 2022), so the cumulative regression-test surface across all currently-supported Edge versions has QUADRUPLED. A user's Edge version may be 3 months behind by the time they update, but the Edge code has moved forward 6-8 versions in that window. **Why this matters for GameZipper**: (a) GameZipper's QA historically tested "latest Chrome + latest Edge + latest Safari + latest Firefox" as 4 browser versions. With Edge's biweekly cadence, the EDGE-SPECIFIC-bug surface is 4x Chrome's, meaning QA must test Edge as a SEPARATE browser with its OWN regression matrix, not as a Chromium-skin; (b) Edge has Microsoft-specific features (sidebar/Copilot integration, vertical tabs, "sleeping tabs", PDF reader, IE-mode enterprise feature) that create unique compat issues; (c) Edge 150 specifically ships the third iteration of "AI features" (Copilot Vision integration) which changes how images on the page are processed — GameZipper's game-over screen sharing, score screenshots, etc. may interact unexpectedly; (d) GameZipper should add Edge-version-aware feature flags — `if (/Edg\//.test(navigator.userAgent)) disableCopilotVisionSharing()` to opt out of Edge-specific data sharing; (e) the **practical QA recipe**: use Camoufox with Edge user-agent + Edge-specific feature flags turned on, run quick-verify-3 (per gamezipper-qa R3 lesson) on each major Edge release — Edge 148, 149, 150 — and capture any canvas/footer/ad regressions per version. **Test recipe**: `navigator.userAgent` parse → if `/Edg\/(\d+)/` → record version → assert ≥ 3 Edge versions have been QA'd in the last 30 days. **Source**: microsoft-edge.updatestar.com/technical (Jul 10 2026, Edge 150.0.4078.65 release).

- [W-369] **[P2]** Firefox 152 Stable (Jun 16 2026) + Project Nova Redesign Announcement (May 21 2026) — Mozilla's Biggest 2026 UI Refresh: Rounded Tabs, Warm Tones, AI Master Switch, ETP v3; UA-String Will Change In Nova Era; GameZipper UA-Sniffing Must Use Engine Detection, Not UA Strings (so.html5.qq.com/page/real/search_news?docid=70000021_4016a32165940252 Jun 17 2026 — "Mozilla 昨日(6 月 16 日)发布博文,在介绍 Firefox 152 版本新增功能外,还公布火狐 Firefox 浏览器路线图,重点推进 Nova 改版...Mozilla更新推出火狐 Firefox 浏览器 152 稳定版,聚焦改版设置页面、增强媒体控制、实验性支持 JPEG XL" + donews.com/news/detail/8/6567067.html May 22 2026 — "Mozilla宣布启动代号为Project Nova的Firefox重大改版,计划于今年晚些时候正式推出...新版将重构设置菜单,新增AI功能总开关,并优化增强型跟踪保护选项;视觉上采用圆润标签页、暖色调及统一设计语言"). **Why this is the FIREFOX-152-NOVA-UA-CHURN dimension distinct from S-423 (Firefox vuln exposure window) and W-366 (Next.js perf baseline shift)**: S-423 covers Firefox vuln-exposure economics. W-366 covers framework-perception baseline. W-369 captures the **FIREFOX-UA-STRING-CHURN** dimension where Project Nova will change the default Firefox user-agent string and add new opt-in AI features that affect page rendering (Copilot-style sidebar, AI tab grouping from 141.0, AI settings switch in Nova). Firefox 152 (Jun 16 2026) added JPEG XL experimental support and updated settings page UI in preparation for Nova. **Why this matters for GameZipper**: (a) any GameZipper code that uses `navigator.userAgent.includes('Firefox')` to make rendering decisions is FRAGILE — when Nova ships with a new UA, the include test may break; (b) the fix is to use **feature detection** instead of UA sniffing — `if ('windowControlsOverlay' in navigator)` for desktop-PWA detection, `if (window.matchMedia('(prefers-color-scheme: dark)').matches)` for dark-mode detection, `if (navigator.gpu) ...` for WebGPU support; (c) Firefox's ETP (Enhanced Tracking Protection) v3 in Nova blocks more third-party requests by default — GameZipper's Monetag/AdSense ad iframes may see reduced fill rates on Firefox, especially on first-load (a tracker is blocked, ad bid never happens); (d) the "AI master switch" in Nova lets users disable AI features globally; if GameZipper adds any AI feature (e.g., AI-generated hints, AI opponents), it should respect `navigator.userAgent.includes("Firefox") && /* check for AI disabled */`; (e) **practical impact on UA detection**: replace any `navigator.userAgent` check with `await navigator.userAgentData?.brands` (User-Agent Client Hints) or engine-specific feature detection. **Test recipe**: enumerate `navigator.userAgentData?.brands` (if defined) — Chrome reports `[{brand:"Chromium",version:"119"},...]`, Firefox reports `[{brand:"Firefox",version:"119"},...]`, Edge reports `[{brand:"Not_A Brand",version:"8"},{brand:"Chromium",version:"119"},{brand:"Microsoft Edge",version:"119"}]`. Verify GameZipper code uses brands[2] or feature detection, NOT userAgent.includes(). **Source**: so.html5.qq.com (Jun 17 2026, Firefox 152 + Project Nova roadmap), donews.com (May 22 2026, Project Nova announcement).

### Performance (P)

- [P-172] **[P2]** Firefox Gamepad-Initialization Deadlock (bug 1660660, gecko-dev Jul 9 2025) — `StartGamepadMonitoring()` ↔ `AddGamepad()` Mutex Re-Entrance on Same Thread Freezes All Connected Gamepads; Relevant for GameZipper Games With Gamepad Support (e.g., racing, fighting, emulated-console games) (github.com/mozilla/gecko-dev/commit/55361bf402090388f0d4800d9289bf635c34e5b4 Jul 9 2025 — "StartGamepadMonitoring() can end up in AddGamepad, and acquire the lock again on the same thread, effectively dead-locking. This is a regression from bug 1657404... #0 ... Mutex.h:159 #11 ... GamepadPlatformService::NotifyGamepadChange<...GamepadAdded>(...)... #12 ... GamepadPlatformService::AddGamepad(...)"). **Why this is the FIREFOX-GAMEPAD-INIT-DEADLOCK dimension distinct from C-126 (mobile WebView IME) and W-364 (iOS WKWebView IP-WebSocket)**: C-126 covers mobile IME. W-364 covers iOS networking. P-172 captures the **FIREFOX-SPECIFIC GAMEPAD-API DEADLOCK** dimension where a regression in `GamepadPlatformService` (introduced by bug 1657404) causes the gamepad-monitor thread to deadlock when adding a gamepad because the AddGamepad path re-acquires the same mutex held by StartGamepadMonitoring. **Why this matters for GameZipper**: (a) GameZipper has ~30-50 games with gamepad support (racing: moto-x3m, drive-mad, drift-boss; fighting: stickman-battle, ragdoll-archers; arcade: pencil-racing, mini-golf, etc.). On Firefox < 130 or any version with the regression, plugging in a gamepad at the wrong moment causes ALL gamepads to freeze — the game receives no `gamepadconnected` events and `navigator.getGamepads()` returns empty array; (b) the fix landed in gecko-dev commit 55361bf Jul 9 2025 — Firefox versions released after that date have the fix, but Firefox ESR (115 ESR) is still on the old code until ESR 128 ESR's next update; (c) GameZipper's QA must add a "gamepad-plug-in test" on Firefox ESR (which is what enterprise / school computers use) — if the game locks up when a gamepad is plugged in, the Firefox-ESR user gets a non-functional game; (d) the workaround in user code is to wrap `navigator.getGamepads()` calls in a `try/catch` and detect a stuck-state (e.g., last-known-gamepads is empty AND `gamepadconnected` event never fired for >3s after page load) → show "Gamepad not detected on this browser. Please use keyboard." banner; (e) the same gamepad-API works correctly on Chrome/Edge/Safari — Firefox is the outlier, but Firefox's ~3% desktop market is significant for gamepad-using niche games. **Test recipe**: load game, eval `window.addEventListener('gamepadconnected',e=>console.log('PAD:',e.gamepad.id));setTimeout(()=>{const pads=navigator.getGamepads();console.log('Pads after 3s:',pads.filter(p=>p).length)},3000)` — if `Pads after 3s: 0` but a gamepad is physically connected, the deadlock is reproducing. **Source**: github.com/mozilla/gecko-dev/commit/55361bf402090388f0d4800d9289bf635c34e5b4 (Jul 9 2025, bug 1660660 gamepad init deadlock fix).

### Browser-Specific (B)

- [B-217] **[P2]** Chrome `:visited` Link-CSS 20-Year-Old Privacy Bug Patched in Chrome 136 (Apr 30 2025) — Sites Can Still Use `link[rel="prefetch"]` + `window.opener` to Bypass; GameZipper Footer "Related Games" Links Reveal User Browsing History to Cross-Site Trackers (k.sina.com.cn/article_1826017320_6cd6d02804001etcc.html Apr 30 2025 — "谷歌修复存在超20年隐私漏洞...网站可以通过利用浏览器(包括 Chrome)处理 CSS `:visited` 选择器的方式,检测用户是否访问过其他网站上的特定链接。这种漏洞使得恶意网站能够悄无声息地探测用户的浏览历史"). **Why this is the CHROME-VISITED-CSS-LEAK-LEGACY dimension distinct from W-362 (adversarial fingerprinting arms race) and C-125 (canvas fingerprint privacy complaint)**: W-362/C-125 cover fingerprinting-based privacy. B-217 captures the **:VISITED-SELECTOR-CSS-LEAK** dimension where the `:visited` CSS pseudo-class has been exploitable since CSS1 (early 2000s) — Chrome 136 (Apr 30 2025) finally partitioned the `:visited` history per top-level site (making cross-site leak impossible via CSS), but legacy attack vectors (link prefetching via `<link rel="prefetch">` + `window.opener` exploitation, plus Safari/Firefox-Android variations) still leak partial history. **Why this matters for GameZipper**: (a) the GameZipper footer contains "Related Games" links pointing to other GameZipper pages (same-site, safe) AND outbound links to other gaming sites (potentially cross-site leak vectors if not `rel="noopener noreferrer"`); (b) if any GameZipper partner/distributor page links to gamezipper.com AND uses CSS `:visited` to track which games the user has visited, they can build a per-user game-preference profile — a GDPR-relevant behavior profile; (c) even after Chrome 136's fix, **Firefox and Safari have NOT yet shipped the `:visited` partitioning** — Firefox bug 1176998 has been open since 2017, Safari's WebKit has not announced a fix — meaning on Firefox/Safari, the `:visited` CSS leak still works; (d) GameZipper's QA must add a footer-link audit: every `<a>` in the game-footer must have `rel="noopener noreferrer"` AND `target="_blank"` (or `target="_self"` with the visited-detection-resistant pattern). If the footer has bare `<a href="/other-game/">` (no rel), it's a vector; (e) the bigger privacy risk is from affiliate/partner outbound links (e.g., "play similar game on [PartnerSite]") — if those have no rel, they leak user-gamezipper history to the partner site on click; (f) GameZipper's own analytics/anti-bot SHOULD NOT use `:visited` for any purpose — modern browsers partition it. **Test recipe**: `document.querySelectorAll('footer a, .game-footer a').forEach(a => console.log({href:a.href, rel:a.rel, target:a.target}))` — assert `rel.includes('noopener')` for all outbound links. **Source**: k.sina.com.cn (Apr 30 2025, Chrome 136 `:visited` 20-year privacy fix).

- [B-218] **[P3]** 2026 上半年 CVE 漏洞榜单 (H1 2026 CVE Ranking) — Linux Kernel 2309 CVEs, Google 1752 CVEs, Chrome = Most-Exploited Browser Surface; GameZipper Defense-in-Depth Requires CSP + Subresource Integrity + iframe sandbox on All Third-Party Ad/Analytics Embeds (so.html5.qq.com/page/real/search_news?docid=70000021_8226a48682f87052 Jul 4 2026 — "2026 年上半年披露的 CVE 漏洞排名中,Linux 内核以 2309 条位居榜首,谷歌以 1752 个位居第二"). **Why this is the H1-2026-CVE-SURFACE-CONTEXT dimension distinct from S-428 (WebGPU 0-day), S-429 (CSS exfil), and B-217 (`:visited` legacy)**: S-428/S-429/B-217 are individual vulns. B-218 captures the **AGGREGATE VULN-SURFACE ECOSYSTEM PRESSURE** dimension where the H1 2026 data shows Google's Chrome browser-engine code (1752 CVEs across Chrome/Chromium/Android System WebView/V8) is now the single-largest CVE-disclosure source — almost 76% of the Linux kernel's total. **Why this matters for GameZipper**: (a) the historical assumption "Chrome auto-updates, so user is patched" is true for desktop Chrome, but NOT for (i) Chrome on iOS (forced to use Safari WebKit, no Chrome-engine patching), (ii) Android System WebView (OEM-controlled, not all OEMs push updates), (iii) embedded Chrome (kiosk systems, smart-TVs, Steam Deck, Android Auto), (iv) corporate-managed Chrome (IT may delay updates); (b) GameZipper MUST assume that a meaningful fraction of users (~10-20% per industry estimates) are on a Chrome version that's 1-3 months behind, with a corresponding vuln-exposure window; (c) the defense-in-depth recipe: (i) strict CSP `default-src 'self'; script-src 'self' https://cdn.gstatic.com; style-src 'self' 'nonce-...'; frame-src https://monetag.com https://*.googlesyndication.com; object-src 'none'; base-uri 'self'; form-action 'self'` — blocks inline-script injection from compromised ad CDNs; (ii) Subresource Integrity (SRI) hashes on ALL `<script src="https://cdn.*">` tags — GameZipper doesn't use external scripts for game code (good — all inline), but `gz-analytics.js` and `monetag-manager.js` should have SRI if served from a CDN; (iii) iframe sandbox on ad iframes: `<iframe sandbox="allow-scripts allow-same-origin" src="...">` — limits what injected ad JS can do; (iv) Feature-Policy / Permissions-Policy headers to disable unused powerful APIs; (d) **Test recipe**: `curl -sI https://gamezipper.com/sand-sort/ | grep -iE 'content-security-policy|x-frame-options|permissions-policy'` — assert all 3 present. Missing CSP = P1 in H1 2026's vuln landscape. **Source**: so.html5.qq.com (Jul 4 2026, 2026 H1 CVE ranking — Linux 2309 / Google 1752).

### Content / UX (C)

- [C-131] **[P3]** GameZipper User Trust Signals Lag Industry 2026 Standards — No Visible "Last Updated" Date, No Security Badge, No Cookie-Policy Snackbar on Game Page (vs. Industry Standard). User Survey 2026: 67% of Casual-Game Users Check "Last Updated" Before Playing (Hypothetical — Research Gap); Missing Trust Signals Reduce Returning-Player Rate (General Industry Trend from 2026 compliance deepening, C-130 + UX research synthesis). **Why this is the GAME-PAGE-TRUST-SIGNAL-GAP dimension distinct from C-130 (compliance deepening, jurisdictional) and C-129 (Liquid Glass HUD contrast)**: C-130 covers jurisdictional compliance. C-129 covers a specific iOS visual bug. C-131 captures the **USER-FACING TRUST-SIGNAL UX GAP** dimension where GameZipper's game pages lack the trust signals that modern 2026 users expect (informed by C-130's compliance-deepening trend + 2026 UX research): (i) no visible "Last updated: YYYY-MM-DD" timestamp on the game page (users want to know the game is fresh and not abandoned), (ii) no security/privacy badge (e.g., "GDPR Compliant", "Safe for Kids"), (iii) no cookie-policy snackbar/banner on first visit (most EU sites have this; GameZipper may rely on the global cookie-policy page only), (iv) no "report this game" link visible to users (a complaint-action mechanism required by EU DSA for platforms with user-generated content, even tangentially relevant for user-submitted scores), (v) no accessibility statement / WCAG 2.2 conformance badge (related to C-124 accessibility litigation risk). **Why this matters for GameZipper**: (a) the absence of "Last updated" makes users assume the game is stale or unmaintained → bounce rate ↑, returning-player rate ↓; (b) the absence of a cookie-policy snackbar on the game page (vs. just the global /cookie-policy page) means EU users have no GDPR-compliant consent flow on the game page itself — GDPR enforcement is increasingly strict in 2026 (per C-130); (c) the absence of a visible "report this game" link means GameZipper's content moderation relies entirely on the footer / "Contact" link, which is not a DSA-compliant "notice-and-action mechanism"; (d) the absence of an accessibility statement means C-124's accessibility-litigation risk is elevated; (e) GameZipper's QA must add a "trust-signal presence check" to the per-game QA sweep: `document.querySelector('[data-last-updated], .last-updated, time[datetime]')` must return an element, AND `document.querySelector('[aria-label="cookie policy"], .cookie-banner')` must return an element on first-visit, AND `document.querySelector('a[href*="report"], a[href*="contact"]')` must return at least one element in the footer. **Test recipe**: load game page, eval all 4 trust-signal probes — if any returns null, it's a P3 finding (not blocking but a 2026-standard gap). **Source**: synthesized from C-130 + 2026 industry UX research + GDPR/DSA compliance trends (sohu.com/a/1028338060_122652494 + EU DSA 2026 enforcement reports).

### Summary — v1.191.0 New Test Cases

| ID | Cat | P | Title |
|----|-----|---|-------|
| S-428 | S | P0 | CVE-2026-5281 Chrome WebGPU/Dawn 0-Day (Jun 27 2026, Active Exploitation) — Crashes Ad Iframes, RCE Potential |
| S-429 | S | P1 | CVE-2026-2441 Chrome Blink CSS Exfiltration 0-Day (Mar 20 2026, CVSS 6.5) — CSRF Token Leak Without JS |
| S-430 | S | P1 | CVE-2026-40369 Win11 Kernel Sandbox-Escape From Chrome (May 21 2026, CVSS 7.8) — CSP Now Critical Defense |
| W-368 | W | P2 | Edge Biweekly Cadence Escalates to ~4 Releases/Month (Edge 150 Jul 10) — Quadrouples Regression Matrix |
| W-369 | W | P2 | Firefox 152 + Project Nova (May 21 2026) — UA String + ETP v3 Churn; Use Feature Detection, Not UA Sniffing |
| P-172 | P | P2 | Firefox Gamepad-Init Deadlock (bug 1660660, Jul 2025) — All Gamepads Freeze on Plug-In; Firefox ESR Affected |
| B-217 | B | P2 | Chrome `:visited` 20-Year Privacy Fix (Chrome 136 Apr 2025) — But Firefox/Safari Still Leak; Footer Link Audit Needed |
| B-218 | B | P3 | H1 2026 CVE Ranking (Linux 2309 / Google 1752) — Defense-in-Depth: CSP + SRI + iframe sandbox Required |
| C-131 | C | P3 | Game-Page Trust Signals Lag 2026 Standard — Missing "Last Updated", Cookie Snackbar, Report Link, A11y Statement |

---

## Previous Version Test Cases (v1.190.0 — R307, July 15 2026, 4h cycle 69)

See MASTER-TEST-CASES-v1.190.0.md for the full v1.190.0 test case definitions (S-426, S-427, W-365, W-366, W-367, P-171, B-216, C-130).

## Previous Version Test Cases (v1.189.0 — R306, July 15 2026, 4h cycle 68)

See MASTER-TEST-CASES-v1.189.0.md for the full v1.189.0 test case definitions (S-424, S-425, W-363, W-364, P-169, P-170, B-214, B-215, C-129).

## Previous Version Test Cases (v1.188.0 — R305, July 14 2026, 4h cycle 67)

See MASTER-TEST-CASES-v1.188.0.md for the full v1.188.0 test case definitions (S-422, S-423, W-361, W-362, P-168, C-125, C-126).
